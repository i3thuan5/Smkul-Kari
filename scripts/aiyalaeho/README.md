# aiyalaeho/：《開會了》編排

原視《'a'iyalaeho: 開會了》的燒印字幕抽成 SRT。這個節目的畫面**同時燒著
族語與華語兩列**，所以交付 SRT 是兩行一條、兩行都來自畫面；沒有語音側
（族語文本畫面就有，不必再辨識一次）。

引擎共用（一行都沒改）：`scripts/ocr/` 切 cue 與出 contact sheet、
`scripts/srtlib/` 組裝鏈與 0.5 秒留白。這裡只放這批語料的編排。

資料正本在 `Kari-SRT/aiyalaeho/`（結構見該目錄的 README）；工作區在
`kithann/out/aiyalaeho/`，全部可重生。

## 檔案

| 檔 | 做什麼 |
|---|---|
| `paths.py` | 語料路徑的單一出處（`stage_path()` 是階段目錄唯一出口，逐集檔案不分層；`check_srt_name` 認 `開會了_<集數3碼>_…`；`--var` 供 shell 取值） |
| `catalogue.py` | 檔名就是這一集：解析集數／族語別／語言別／語言代號，登記成 pending 條目。**不讀** `ilrdf-corpus.csv` |
| `verify_band.py` | 切 cue 前逐集驗版型：量帶的顏色與字幕列的位置 |
| `ingest.py` | 視覺辨識 TSV 的驗證匯入（cue↔sheet 歸屬、空白列補尾端 tab、只吃 `b*.tsv`） |
| `make_srt.py` | 單集組裝：每條兩行「族語：／華語：」，走共用組裝鏈 |
| `make_all.py` | 整批組裝＋進度表工作版 |
| `tracker.py` | `smkul.csv` 的九欄與單列組法，三方共用 |
| `publish.py` | 整批把關→遷時間軸→清 pending→定版 `smkul.csv` |
| `rebuild.py` | 只用 store 離線重建全部交付 SRT，逐 byte 驗證 |
| `presets.json` | 版型：`aiyalaeho-bilingual`（黃底雙列帶） |
| `brief.md` | 視覺辨識讀者判準的**正本**——每一批的提示都以它為準，改一次後面每批都跟著改 |

news 有而這裡**沒有**的四支，各有理由：`fetch_sftp.sh`（素材已在本機，
少數幾支手動 `scripts/news/sftp.sh get` 就好）、`plan_month.py`（沒有月份
批次，登記併進 `catalogue.py`）、`gap_sheets.py`（沒有 `.work`／`.B.work`
之分，`cues --sheets` 首輪就是完整的）、`batches.py`（`ocr.cli pending`
已列未讀的 sheet）。

## 跑法

```bash
# 1. 登記（檔名驅動；-n 先看要做什麼）
python3 -m scripts.aiyalaeho.catalogue -n
python3 -m scripts.aiyalaeho.catalogue

# 2. 逐集：驗版型 → 切 cue（出 contact sheet）→ 邊界精修
PY=$(python3 -m scripts.aiyalaeho.paths --var VENV_PY)
V='kithann/開會了/068-阿美語-秀姑巒-雙語字幕.mp4'
W="$(python3 -m scripts.aiyalaeho.paths --var WORK)/開會了_068_Amis_阿美.work"
$PY -m scripts.aiyalaeho.verify_band "$V" --quiet && \
$PY -m scripts.ocr.cli cues "$V" -o "$W" \
    --presets scripts/aiyalaeho/presets.json \
    --preset aiyalaeho-bilingual --sheets < /dev/null && \
$PY -m scripts.news.refine_cues "$V" "$W/1-cues/cues.json"

# 3. 視覺辨識：subagent 一批讀 24 張 sheet，TSV 直接寫進
#    Kari-SRT/aiyalaeho/1-ocr/2-vision/<srt_name>/bNN.tsv
python3 -m scripts.ocr.cli pending "$W"        # 猶未讀ê sheet
python3 -m scripts.aiyalaeho.ingest 開會了_068_Amis_阿美

# 4. 組裝、定版、驗收
python3 -m scripts.aiyalaeho.make_all
python3 -m scripts.aiyalaeho.publish
python3 -m scripts.aiyalaeho.rebuild --verify
```

`cues` 那步的 `< /dev/null` 不可省：迴圈裡的 ffmpeg 會把 stdin 吸乾，
切 cue 就停在半路而且**不會報錯**（新聞那邊踩過，cues 少四倍）。

**時間軸分兩階段放，餵給 `refine_cues` 的是粗切那一份。** `cues` 寫
`$W/1-cues/cues.json`（0.2 秒格線，寫一次就不再動），`refine_cues` 讀它、
把精修的寫到 `$W/2-refined/cues.json`。要讀「這個 work dir 最好的那一份」
用 `scripts.datadirs.cues_to_read(work)`，不要自己拼路徑——它會挑精修的，
沒有才退回粗切的。2026-09-06 踩過：這份文件原本寫的是舊布局的
`$W/cues.json`，照著跑，切 cue 那步成功、refine 當場 `FileNotFoundError`。

**要重切一集之前，先確認上一輪的 `refine_cues` 已經結束**（2026-08-31
踩過）。`cues` 跑完會接著跑 refine，refine 是把 `cues.json` 讀進記憶體、
改完再整份寫回去。這時候若另開一輪 `rm -rf` 掉 work dir 重切，舊的 refine
不會死，它會在幾分鐘後把**舊的 manifest 寫進新的 work dir**——manifest 說
一套、磁碟上的 strips 與 sheets 是另一套，而且沒有任何地方會報錯。查法：

```bash
ps -eo pid,etime,cmd -ww | grep -E '[o]cr.cli cues|[r]efine_cues'
```

## TSV 格式（視覺辨識的交卷）

每個 cue 兩逝，行尾允許留空（那是「這列沒有字幕」，不是漏讀）：

```
12	formosan	Nga'ay ho^
12	han	大家好
13	formosan
13	han	這也就是今天的原因
```

族語列**照畫面抄**：夾漢字就寫漢字（`qau aicu a 民族議會 mana…`），
`^ ' " : ʉ` 這些符號原樣保留，`'`（一撇）與 `"`（兩撇）是不同的符號、
不可互換。

## 素材：38 集有字幕、30.9 點鐘（量過ê，2026-08-31）

- SFTP `族語節目/開會了/`：**44 支 mp4**——41 支是 2026-08-04 頂傳ê
  `NNN-語言[-方言]-字幕狀態.mp4`，另外 3 支是三月ê舊頂傳（`116ALL_無字`、
  `119-混雜`、`122-混雜`）。44 組 mp3／wav 用袂著（這爿無做 ASR）。
  本機 `kithann/開會了/` 有 8/4 彼批 41 支，位元組數逐支對會著，共 68 GB。
- **`開會了_a_iyalaeho=上字文稿/` 佮影片零交集**：文稿是第 001–045 集，
  影片是 068–164。**袂當供字、嘛袂當對照**，欲收是另外一案ê文本語料。
  （莫閣去揣——查過矣。）
- **分類**：**雙語交付 37 集**（30.1 h）；**字幕版型異常 4 集**——083
  （僅華語字幕）、088／090／098（無字幕），閣加 116（未下載）；混雜
  119／122（未看過，愛走完 SOP 才知）。異常彼幾集**無切、無讀、無交付**，
  列佇 `Kari-SRT/aiyalaeho/smkul-字幕版型異常.csv`，理由記佇第十欄。

  **083 本底交過 694 行**，2026-09-05 撤轉來：伊ê華語列 1,174 條讀了
  好好，毋過**族語列 0 條**（qc ê `formosan_rows: 0`）——本語料ê交付品
  是族語文字，伊無。彼 17 个 TSV 佮 SRT 已經對 store 提掉；欲揣ê時
  佇子模組 commit 694c709 ê歷史內底，工作目錄嘛猶有 `transcripts.json`。
- **語言分布**（38 集，檔名為準）：阿美 8（068 秀姑巒、112／113 南勢、
  082／084／093／105／117 無註）、泰雅 6（091 賽考利克）、排灣 5、布農 4、
  雅美 4、賽夏 3、卑南 3、魯凱 2（083 非霧台、095 霧台）、賽德克-德路固 1
  （102）、拉阿魯哇 1（107）、太魯閣 1（123）。另外 108「講中文居多」、
  111「很多人講中文」。
- **規模**（由 068 推：52.5 分、637 cue、169 sheet）：1,858 分 → 約
  22,500 cue、約 6,000 sheet（3.8 cue／張）、約 250 批。切 cue ＋精修
  約 4 h；work dir 約 20 GB。
- **視覺辨識準確率**（`OCR辨識方案分析.md`，開會了 200 條）：Sonnet 5
  族語 98.8%／華語 98.5%、Opus 5 96.9%／99.5%、Fable 5 100%／98.5%；
  `^ ' " :` 三个模型攏 229/229。

**目錄（`ilrdf-corpus.csv`）對開會了是落後ê，所以無讀伊。** 46 列
（79–124）ê年度／日期／時段／族語別攏空，43 列標「無影片」毋過伺服器
有 mp4，068 佮 164 連列都無。xlsx 正本ê「開會了」分頁干焦有序列／節目
名稱／集數／備註。**開會了是予資料夾檔名清單驅動ê**（`catalogue.py`）。

**播出日期查無，所以 srt_name 無日期。** 揣過ê所在：`ilrdf-corpus.csv`
佮 xlsx 正本、mp4 metadata（8/4 彼批 libx264 重轉 `Lavc60.31.102`，
`creation_time` 洗掉矣）、原視新聞網系列頁（逐集有發佈日期，毋過「第N集」
是網站家己ê列表序，對袂轉播出集數）、原文會 VOD（無這个節目）、維基
節目列表（404）、YouTube（頁面日期掠無）。干焦掠著一寡：第 123 集
2022-09-13 捌佇節目表（透早重播）→ 79–124 大約 2020–2022、164 閣較晏。
日後若原語會補權威日期表，進度表加一欄記入去就好——**srt_name 莫振動**。

**版型 38 集一致，毋過規條帶會漂 ±12 px。** 12 个取樣點 × 38 集ê列剖面：
068 是族語 896–930／華語 954–991，大部份 ~893–929／952–989；偏上濟ê是
079（900–935／959–996）、102（904–939／962–1000）、118（903–943／
950–1004）、121（907–943／966–1003）、111（904–942／969–1008）、117
（918–947／966–1008）、094（—／970–1011）。117／111 ê華語列貼槽底、094
超出 3 px，所以華語槽才放到 h=64（**莫閣放大**，看下跤「裝箱預算」）。

## 重切進前愛先確認頂一輪ê refine 死了

`cues` 走煞會紲落去走 `refine_cues`；refine 是kā `cues.json` 讀入記憶體、
改了才**規份寫轉去**。這站若閣開一輪、閣 `rm -rf` 掉 work dir 重切，
**舊ê refine 袂死**——伊過幾分鐘會kā舊ê manifest 寫入**新ê** work dir，
manifest 講一套、磁碟頂ê strips 佮 sheets 是另外一套，**無一个所在會報錯**。

2026-08-31 就按呢：h=66 彼輪ê refine 走七分鐘矣，我 h=64 彼輪kā work dir
刣掉重切，兩隻做伙搶 CPU（切 cue 慢一半），閣險仔予舊 manifest 蓋著。

查法（一擺干焦准一條鏈）：

```bash
ps -eo pid,etime,cmd -ww | grep -E '[o]cr.cli cues|[r]efine_cues'
```

## 帶ê懸低逐集無仝：兩个 preset

規批切煞（38/41）了後，賰三集予 `verify_band` 擋落來，攏毋是仝一款：

| 集 | 帶ê色 | 判定 | 是啥 |
|---|---|---|---|
| 098 | 0.9 倍 | MISMATCH | 檔名是「無字幕」，量著ê是版型無合（戶外亮景，帶區下沿有對比）——兩爿攏指向無字幕 |
| 094 | 2.4 倍 | MISMATCH | 有帶，毋過**規條帶低 ~14 px** |
| 087 | 7.0 倍 | MISMATCH | 仝款低，閣加兩逝倚傷近 |

量規集ê列剖面（逐集 750 格）：

| 集 | 族語逝 | 華語逝 | 兩逝中央ê谷 |
|---|---|---|---|
| 087 | 918–943 | 966–1003 | y=962 |
| 094 | 923–951 | 974–1011 | ~962 |

標準 preset ê槽是 888–948／948–1012，**分界 948 拄好切佇 094 族語逝ê
中央**。所以加一个 `aiyalaeho-bilingual-low`：分界徙到 **962**，槽是
902–962 佮 962–1014（華語槽貼到 region 底，因為 094 ê字底到 1011）。
094 用新 preset 就 `OK`。

**087 猶原 MISMATCH，毋過是閘門ê限制，毋是版型無仝。** 伊兩逝倚傷近，
族語ê下伸部（`marekaytayan`、`zazekazekalrziya` ê `y`）伸入去列間ê縫，
`runs_of` ê列偵測kā兩逝黏做一逝（918–992）。我抽影格看過——**是標準ê
兩行版型**，而且兩逝各自落佇新 preset ê槽內。所以 087 用
`--preset aiyalaeho-bilingual-low` 切，閘門彼關是靠量測佮影格人工確認ê。

**後來揣著機械ê解法：取樣加倍。**（2026-09-05 量ê。）087 用低版 preset
配無仝ê取樣長度：

| 取樣 | 字幕逝 | 剖面對比 | 判定 |
|---|---|---|---|
| 240 格（預設） | 918..992（兩逝黏做伙） | 4.9 倍 | MISMATCH |
| **480 格** | **918..943、967..1004** | 10.0 倍 | **OK** |
| 750 格 | 918..943、967..1004 | 14.2 倍 | OK |

平均ê格數加倍，下伸部橋過空縫彼幾格ê份量就予正常格淡去，兩逝就分會開。
480 佮 750 結果一模一樣，所以 **480 就夠**。

**所以 SOP 是四步，攏是機械ê，免人判**：`verify_band` 回 1 ê時，照順序
試 `--preset aiyalaeho-bilingual-low`、`--duration 480`、兩个鬥做伙；任何
一步回 0 就用彼組參數切（`cues` 嘛用仝一个 preset）。四款攏回 1 才是真正
ê版型不符。若無這一層，087 這款**已經交付 590 行ê好集數**會予人記做
「版型不符」擲掉。

（仝一款「下伸部橋過空縫」ê現象，086 彼爿是kā接縫窗撐懸——看
`brief.md`「接縫」彼節。）

## 判準ê門檻莫一直捋——愛換主證據

`verify_band` 頭起先ê門檻是**三集**校ê（068 15、083 22、164 8），走到
第三集 081 就予人拍破：兩逝字幕明明各佇家己ê槽（904–929、953–990），
毋過帶ê比才 2.0，予門檻 2.5 擋落來。因端是彼集ê攝影棚本身足溫（燒色），
分母大起來。16 集了後ê分布是：有帶 2.0–22、無帶 0.8–1.0。

**治標是kā門檻園 1.5；治本是改結構**：

```
舊：帶ê色 ≥ 門檻  →  才閣看位置
新：位置合槽      →  ok（帶ê色干焦印出來做佐證）
    位置無合      →  剖面對比懸＝mismatch、平坦＝no-band
```

**一个無咧做判定ê量測，就袂判毋著。** news 彼爿ê session 查出in ê
`SPIKE` 門檻嘛是四集校ê，而且in ê失敗方向佮我**相反**（比值低 → 判做
「無紅帶」→ 跳過檢查 → 放過歹檔案；我這爿是誤擋，吵毋過安全）。

**教示**：樣本少ê門檻，第三集就會予人拍破；判準會用著ê時，樣本愛涵蓋
「歹判ê彼爿」（這擺是溫色攝影棚）。閣有——拄著邊仔案例愛想ê是「換一个
主證據」，毋是一直捋門檻。

（順紲：測試內底有幾條合成案例本底傳「有字幕逝ê形狀＋平坦ê剖面」，彼是
真實袂發生ê組合——一逝字就是一个峰。合成資料愛合物理。）

## 版型判定會過期——判準改了愛閣走一遍

批次腳本ê `BAND-FAIL` 是 `continue`：彼集就**恬恬予人跳過**，無錯誤、
無重試，尾溜規批做煞你才發現少一集。081 就按呢跳過去——03:56 用舊判準
判做 MISMATCH（門檻 2.5），04:15 我kā `verify_band` 重構做「排版位置是
唯一ê閘門、帶ê色水干焦是佐證」，081 用新版是 **OK**，毋過**log 是舊ê**，
腳本無閣走一遍，就一直跳過。

**做法**：判準若改，`batch_cut.sh` **閣走一遍**——伊本底就是設計來續跑ê
（有 refined `cues.json` ê跳過），重走一擺就會kā頭前予舊判準擋落來ê
掠轉來。莫干焦看 log ê結論，愛看 log ê**時間**佮 checker ê mtime 對會
起來袂。

## 一集交出去了後：全語料掃碼位（三分鐘，掠著過真ê代誌）

每一集 ingest 煞，kā已交ê TSV 攏總掃一遍標點ê碼位。這是**協調ê人**做
ê，讀者無法度做——伊愛看規个語料。

**掃啥**：撇號類（`'` U+0027、`’` U+2019、`‘` U+2018）、引號類（`"`
U+0022、`“` U+201C、`”` U+201D、全形ê `＂`／`＇`）、橫槓類、間隔號類。

**看啥**：

1. **彎ê引號愛成雙**：`“` ê數愛佮 `”` 相仝。無仝就是有一爿判毋著——
   抑是畫面本底就無對稱（彼款愛有讀者佇報告內底講過）。
   實例：掃著 `”` 3 个、`“` 2 个，**差彼一个拄好就是 103 b02 cue 173**
   ——彼位讀者已經家己標出來講「畫面上真ê無開引號」。**讀者ê判斷佮
   規語料ê統計獨立對著**，兩爿互相做證。
2. **孤一款ê物件愛去看**：某一个碼位規語料才出現一兩擺ê，通常毋是
   拄仔好，是誤讀。096 cue 638 彼个**唯一ê**簡體 `统`（對 321 个 `統`）
   就是按呢揣著ê，量了確定是 `統`。**「規語料才這一个」本身就是
   一支旗仔。**
3. **橫槓**已經定案矣：100% U+002D，出現別款就是有問題。

**掃法**：

    python3 -c "
    import glob, io, collections
    M = u"'’‘\"“”＂＇"
    tot = collections.Counter()
    for f in glob.glob('Kari-SRT/aiyalaeho/1-ocr/2-vision/*/b*.tsv'):
        for ch in io.open(f, encoding='utf-8').read():
            if ch in M:
                tot[ch] += 1
    for ch, n in tot.most_common():
        print('U+%04X %s %d' % (ord(ch), ch, n))
    "

## 已知缺陷：長 cue 內底藏著幾若句字幕（猶未補）

切 cue 對這批素材會**切傷少**：一條 cue 內底有時陣khǹg幾若句字幕，圖條
是這幾句ê合成，讀者干焦讀會著佔上久彼句，其他ê**規句對時間軸消失**。
量著ê實例（083 三位讀者對母帶逐格掃出來ê）：

| cue | 時長 | 內底實際有幾句 |
|---|---|---|
| 083 cue 196 | 13.4 s | 4 句 |
| 083 cue 297 | 10.3 s | 4 句 |
| 083 cue 184 | 11.4 s | 3 句 |
| 083 cue 415 | 9.9 s | 4 句 |

**讀者連紲掠著ê實例**（逐个攏有時間，通做補救ê種子資料）：

| 集 | 無去ê字幕 | 時間 | 是按怎無去 |
|---|---|---|---|
| 083 | 以及歷史 | 29.6–31.2 | 藏佇 cue 12 |
| 083 | 讓我們聚在這裡 | 31.2–33.2 | 藏佇 cue 12 |
| 083 | 習俗也不同包括歌謠 | 138.8–141.4 | 藏佇 cue 53 |
| 083 | 我們要儘快 | 2213.2–2214.1 | 藏佇 cue 867（三句khǹg做伙） |
| 083 | 一起協助 | 2228.6–2229.4 | 藏佇 cue 875 |
| 083 | 我們替下一代感到憂心 | 2422.1–2428.7 | cue 957（11.6 s 窗） |
| 083 | 還說暑假有一個月 | 2448.1–2450.1 | cue 966/967 |
| 083 | 原來孩子們的學習能力 | 2454.3–2457.8 | cue 967（13.2 s 窗） |
| **094** | **`aw`／`是`（雙行）** | **1989.95–1990.85** | cue 555 ê尾溜（555 彼句佔 4.2 s） |
| **094** | **`他們是一體的`（跨列單行）** | **1996.45–1998.05** | 跨佇 cue 556 尾溜佮 557 頭 0.1 s |

094 b05 是用母帶 5 fps 逐格分段掠著ê，伊照判準「寫佔上久彼句」處理，
閣kā損失報出來。**讀者會家己掠著這款**，毋過in無法度佇 TSV 內底修
——彼是時間軸ê問題，愛補救彼條線。

**規模愛用著ê尺量。** 我頭起先用「cue 時長 ≥6 秒」ê比例算，看起來逐集
攏破病（068 26.7%、082 18.1%）——**彼是過度推論**。新聞彼爿ê `blind_cues`
判準較準：`frames × 0.2` 是這條 cue **真正予人看過**ê秒數，`end − start`
是伊宣稱涵蓋ê，兩者ê**差額**才是無人看過ê所在。伊ê函式（`unseen`／
`risky`／`summary`）是純ê，食 cue dict ê list，會使直接提來量 aiyalaeho：

| 集 | cue | 愛重看 | 其中干焦因為「長」 | **未看秒數** |
|---|---|---|---|---|
| 068 | 637 | 170 | 170 | 5 |
| 079 | 361 | 68 | 68 | 3 |
| 082 | 652 | 118 | 118 | 4 |
| **083** | 1181 | 123 | 84 | **94** |
| 084 | 596 | 105 | 103 | 8 |

068／079／082／084 ê旗仔**規遍**是「時長 ≥6 秒」插ê，真正無取樣著ê時間
合計才 3–8 秒——彼款長 cue 大部份是一句字幕本底就khǹg遐久。**083 無仝**：
39 條是 `unseen` 命中、未看 94 秒，是別集ê 20 倍，所以讀者才會佇遐一直
掠著漏句。

**規个已交ê語料量落去，這个形出現了閣較明。** 兩个閘門插起來ê內底，
**`unseen` 彼道幾乎無振動**：逐集「上大未看秒數」是 0.2–5.7 秒，干焦
083 有 5.7，其餘攏佇 3 秒下底。旗仔差不多**全部**是 `LONG` 插ê。
按呢就知影：**這爿ê切 cue 對取樣涵蓋率無問題，問題是「一條 cue 內底
khǹg幾若句」**——彼是 `LONG` 想beh掠、毋過掠袂準ê物件（新聞彼爿量ê
精確度：`spread` 這款排序型ê判準，60 條會噴 38 條假ê）。

**按呢重算**（隨時會綴交付ê集數變，莫記死；`unseen`／`risky`／`summary`
是純函式，食 cue dict）：

    PYTHONPATH=. python3 -c "
    import glob, json, os
    from scripts.news import blind_cues as bc
    for d in sorted(glob.glob('kithann/out/aiyalaeho/*.work')):
        m = json.load(open(os.path.join(d, 'cues.json'), encoding='utf-8'))
        s = bc.summary(m['cues'])
        print(os.path.basename(d), s['cues'], s['risky'], s['long'])
    "

**毋過 `blind_cues` 彼兩个閘門，佇這爿ê召回率是 12／25。** 我kā五位讀者
對母帶掃出來、實地確定「這條內底有幾若句」ê 25 條 083 cue 提去試，掠著
12 條。漏去ê 13 條分做兩類，兩个閘門攏無模型著：

| 漏法 | 例 | 是按怎兩个閘門攏無響 |
|---|---|---|
| **短ê騎線 cue** | 746（0.8 s）、906（1.3 s）、966／984（各 1.0 s） | 比一句字幕閣較短，`unseen` ≈ 0、時長閣短 |
| **5 秒頂下藏幾若句** | 12（5.6 s 藏 3 句）、867（4.9 s 藏 3 句）、889（4.6 s） | `unseen` ≈ 0，時長閣落佇 `LONG = 6.0` 下底 |

**試過ê第三个判準：cue 家己內底ê內容有振動無**（`spread` ＝這條 cue
逐格ê遮罩佮頭一格ê距離，取上大彼个）。愛配**收緊ê遮罩**才有路用：

| 遮罩 | 讀者掠著ê（中位數） | 對照組（中位數） | 門檻 0.25 |
|---|---|---|---|
| 原本 `max_spread=50` | 0.382 | 0.324 | 掠 19/25，誤報 41/60 |
| **`max_spread=20`** | **0.570** | 0.336 | **掠 22/25**，誤報 38/60 |

**結論：這个判準會使做「排序」，袂使做「閘門」。** 誤報 63% 傷懸
（讀者實地掃出來ê真正比例是 8–10%），毋過kā 25 條內底ê 22 條排入去
頭前——`blind_cues` 本底ê輸出就是「上歹ê排頭前」，拄好合。補救ê時
用「收緊遮罩ê `spread` 排序 ＋ 對母帶重讀頭前彼幾成」，莫用門檻切。

補救ê路新聞彼爿行過矣（`scripts/news/README.md`ê「切 cue 失效時的補救」）：
`blind_cues` 揀出可疑ê cue、`reread` kā伊切做「一句一段」、`resplit` 寫入
時間軸；閣有 `rescan_band`（規段重切，會kā後壁全部 cue 重編號——五个所在
攏著愛綴咧改，是規條管線上危險ê一步）、`split_cue`、`blank_runs`。

**試過ê、無效ê**（莫閣行這條）：

| 試ê | 結果 |
|---|---|
| 降 `change` 門檻（0.35→0.30／0.25／0.20／0.15／0.12） | 上好 73%（原本 67%），閣落去顛倒較䆀 |
| 收緊遮罩 `max_spread`（50→20，遮罩墨水對 18,417 落到 9,459、換句 Jaccard 對 0.357 起到 0.571） | 邊界回收率猶原 ~70% |

對照組講予清楚：068 家己ê華語單行段（cue 398–465）是 **69 个換句點干焦
1 个無對著**——所以毋是「單行版型」ê問題，是彼幾條長 cue。

**猶未做**：aiyalaeho 這爿無 `blind_cues`／`reread`／`resplit`（開頭ê盤點
刻意無複製）。交付進前愛補這條線，抑是kā news 彼三支借過來改。

## 版型與判準（量過的）

38 集有字幕的版型一致：畫面底部黃紅漸層不透明帶，族語列在上、華語列
在下。preset 的槽是族語 y 888–948、華語 y 948–1012（開到 1012 是因為
094／111／117 三集的字腳落在 1008–1011；不開到區底 1014，是因為 strip
高度會吃 contact sheet 的裝箱預算——66 px 時一個 cue 的區塊 560 px、
預算 558 px，每張就從 4 條掉到 3 條，為兩個像素多花三分之一的閱讀量）。

sheet 的裝箱預算是 `1.10 MP ÷ sheet 寬`，而 sheet 寬是
`108 + 該集最寬的 ink 圖條 + 16`——**逐集不同**。068 是 1970（預算 558），
滿版的集數會到 2044（預算 538）。目前的列高（60＋64、block 138）在
1970 下裝 4 條、在 2044 下裝 3 條，窄的集數可以到 6 條。

**族語槽不再縮短**，即使縮 4 px 就能保證每張 4 條：族語列的 `^` 標記
就位在字身最上緣，槽頂只剩 1 px 餘裕時，削掉的正好是這批語料最在意的
符號。成本差異（三分之一的閱讀量）換不過拼寫正確。

`verify_band.py` 量兩件事，判定只用其中一件：

- **字幕列的位置（主證據，唯一的閘門）**：每個量到的文字列都要完整落在
  preset 宣告的對應槽內、一個槽一列。
- **帶的顏色（報告用，不做判定）**：逐列「帶色」（飽和、R≥G≥B）像素的
  比例，區內下沿比區外上方。有帶 2.0–22 倍、無帶 0.8–1.0 倍。它本來是
  閘門（門檻 2.5、用三集校的），整批跑到第三集就誤擋 081——那集兩列
  明明各在槽內，只因帶上方是暖色攝影棚把分母墊高、比值掉到 2.0。
  **一個不做判定的量測就不會判錯**，所以現在只印出來給人看。
- **字幕列**：文字遮罩的逐列 ink，**只看帶罩到的列**。083 的帶比較短，
  帶上方的亮攝影棚畫面曾被誤判成 48 列的「字幕列」——畫面不是字幕。
  ink 門檻是絕對值 50（實測字幕列 83–453、無字幕集的雜訊 8–37）；用
  相對門檻會把安靜的華語列洗掉（164 就是）。

三種判定：`OK`（有帶、每列各在自己的槽內）、`NO-BAND`（沒有帶也沒有
有結構的墨水——88／90／98 這種本來就沒字幕的集，照切、交付 0 行 SRT）、
`MISMATCH`（有帶但列跑出槽外，或沒帶卻有對比 ≥2 倍的墨水——新聞版型
量到 4.04，就是這條擋下來的）。
