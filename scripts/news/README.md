# 族語新聞 burned-in 字幕 → SRT

把原視族語新聞（`族語新聞/` 的 mp4 與 2 月的 mxf 母帶）裡燒在畫面上的
中文字幕抽成 SRT。影像處理引擎是 `scripts/subs2srt/`（做法說明見
`.claude/skills/video-subtitle-srt/SKILL.md`）；這個目錄放這批語料專屬的
編排：檔名對應、preset、路徑設定（`paths.py`）。

三個位置各司其職：

- **這裡（`scripts/news/`）**：程式與 `presets.json`、`inventory.json`。
- **`Kari-SRT/` submodule**：資料正本——交付 SRT（`news/1-ocr/3-srt/`）、
  `news/smkul.csv`、每集時間軸 `news/1-ocr/1-cues/`、視覺逐字稿
  `news/1-ocr/2-vision/`；逐集檔案都在階段目錄下的
  **播出月份**一層（`1-cues/2021-02/<srt_name>.json`）。僅靠主 repo + Kari-SRT
  即可離線重建全部 SRT：`python3 -m scripts.news.rebuild --verify`
  （或 `tox -e rebuild`）。**每次程式修改完在本機跑一次**——
  Kari-SRT 是私有 repo，CI 抓不到 submodule，這條不進 CI。
- **`kithann/`**（gitignore）：來源資料與可重生快取（work dir、log）。

### `kithann/` 每次執行後會有什麼

不算舊資料（FFmpeg／kaldi／開會了／ilrdf-corpus 那些既有素材），pipeline
往後每跑一次，只會在 `kithann/out/` 底下寫東西，而且全部是快取／日誌，
沒有任何一份是正本：

| 路徑 | 何時產生 | 內容 | 性質 |
|---|---|---|---|
| `out/mxf/<slug>.work/` | `fetch_sftp.sh`／`run_cues.sh` 跑 `cues` 步驟 | `cues.json`（時間軸）、`sheets.json`、`sheets/*.png`、`strips/*.png` | 快取，遷入 Kari-SRT 後可刪 |
| `out/mxf/<slug>.work/transcripts.json` | 有跑 `ocr --engine tesseract` 才有（非預設）| tesseract 辨識草稿 | 快取，不供字，可刪 |
| `out/mxf/<slug>.B.work/` | `gap_sheets.py` 出 contact sheet | `cues.json`、`sheets.json`、`transcripts.json`、`verified.json`；`strips` 是 symlink 回 `.work/strips` | 快取，可刪 |
| `out/smkul.csv` | `make_all.py` | 進度表**工作版**（含還沒做完的集數）| 快取，可刪；交付版由 `publish.py` 寫進 Kari-SRT |
| `out/mxf-logs/<slug>.get.log` | `fetch_sftp.sh` 下載階段 | SFTP `get` 輸出紀錄 | 日誌，可刪 |
| `out/mxf-logs/<slug>.cues.log` | `fetch_sftp.sh` 切 cue 階段 | `cues` 指令 stdout/stderr | 日誌，可刪 |

兩點容易誤會：原始影片不會留在這裡——`fetch_sftp.sh` 下載到 `kithann/out/stage/`（同名同位元組數會重用；放 `影片名.keep` 可留給別的 session，誰放誰刪），
`cues.json` 一寫出來就刪片。視覺辨識的 TSV 也不會寫進這裡——`ingest.py`
預設直接讀寫 `Kari-SRT/news/1-ocr/2-vision/<年-月>/<srt_name>/`，
`kithann/` 這邊的 `verified.json` 只是本地追蹤「這個 work dir 核實到哪」
的快取。

**現況：交付的每一個 cue 都由 Claude 視覺辨識供字**，tesseract 與文稿都
不供字。做到哪裡看正本，不要在這裡記數字（會隨批次變）：

```bash
python3 -m scripts.news.rebuild --verify   # 通過的集數＝已交付集數
```

集數、cue 數、族語涵蓋一律以 `Kari-SRT/news/smkul.csv` 與
`Kari-SRT/news/inventory.json` 為準。第一批（2021-02）的來由值得記著：
本機 24 個 mxf 母帶扣掉 2 個上傳不完整，另從 `110.1-110.10/7月/` 的 mp4
補齊族語涵蓋——其中 037晚 排灣就是那兩個壞檔之一，mp4 版本完整、救回來
了；041午 鄒兩個來源都是同一份短檔，照做但標 `partial`。

## 跑法

流程是**三個階段**，各自吃的資源不同——排時間、看卡在哪，都是照這
個分：

| 階段 | 入口 | 吃什麼 | 一集約略 |
|---|---|---|---|
| **一、cues** | `fetch_sftp.sh`（下載 → 驗字幕帶 → 切 cue → 精修） | 本機 CPU＋網路 | 下載 5 分、切 cue 6 分、精修 8.5 分 |
| **二、OCR** | `/smkul-news` 的視覺辨識 → `ingest` → `make_all` → `publish` | **Claude 視覺辨識**（模型呼叫）；前後的匯入與組裝是秒級 CPU | 約 50 分（178 張 sheet、8.2 批）|
| **三、asr** | `asrmt_batch`（抓音檔 → vosk → 投影 → render） | 本機 CPU | 約 15 分 |
| **四、品質** | `asrmt_run --step mt` → `judge` → sonnet subagent → `ingest` → `--second` → fable subagent → `ingest` → `quality` | ai-labs 服務（免費、單併發）＋**Claude 判定** | 翻譯約 15 分、判定約 8 批 |

**時間的大頭在第二階段**：一個月 71 集 ≈ 58 小時視覺辨識，而第一階段
整月約 7.5 小時。第三階段跟第二階段可以並行。封存 mkv
（`transcode/archive_batch.py`）是平行支線，不在交付的關鍵路徑上。

**新的月份（從 SFTP 抓）**——兩行指令，或用 `/smkul-news` 這個 slash
command。單位是**播出月份**，不是資料夾：

```bash
python3 -m scripts.news.plan_month 2021-03 -n   # 先看要做哪些、跳過哪些
python3 -m scripts.news.plan_month 2021-03      # 登記（pending）
bash    scripts/news/fetch_sftp.sh 2021-03      # 先加 --limit 2 試跑
```

之後接第 3 步（視覺辨識）與第 4 步（組裝）。

**已在本機的影片**（2 月那批 mxf 就是這樣做的）：

```bash
python3 -m scripts.news.add_episodes <路徑>… # 1. 影片 → 節目資料 → SRT 檔名
bash    scripts/news/run_cues.sh                 # 2. 切 cue（約 2.5 小時，I/O 綁死）
# 3. 視覺辨識：subagent 一批讀 24 張 sheet、直接把 TSV 寫進磁碟
python3 -m scripts.news.ingest <slug> <tsv 目錄>   # 驗證＋匯入
python3 -m scripts.news.make_all          # 4. 產 SRT（進度表寫 kithann/out/）
python3 -m scripts.news.publish           # 5. 遷資料＋定版 smkul.csv
python3 -m scripts.news.rebuild --verify  # 6. 驗離線重建
```

**補特定幾集**（不是整個月，例如把某幾個族語從 0 集補到 2 集）：

```bash
# 1. 只抓指定的幾支：--only 是對「檔名」比對的 ERE
bash scripts/news/fetch_sftp.sh 2021-02 \
     --only '^(21NL005_37晨間|21NL004_37晚間)族語新聞\.mp4$'
# 2. 把這幾集寫進 inventory（影片已經刪掉了，所以只能用檔名查目錄）
python3 -m scripts.news.add_episodes '族語新聞/110.1-110.10/7月/21NL005_37晨間族語新聞.mp4' …
# 3. 出 contact sheet（全部 cue 都上）
python3 -m scripts.news.gap_sheets <slug> …
# 4. 視覺辨識 → ingest → make_all（同上）
# 5. 整批做完才能 publish：清 pending、遷 cues、定版 smkul.csv
python3 -m scripts.news.publish
python3 -m scripts.news.rebuild --verify
```

`--only` 存在的理由是磁碟：一個月動輒上百支、幾百 GB，補三支不該用
`--limit` 從頭數。`add_episodes.py` 存在的理由是**一集一集指定路徑**這條
路仍然要有：`plan_month.py` 依規則挑不出來的集數（全語料 8 集）判定之後
就走這條補做，被寫掉的來源找回來時也是。已經在 inventory 裡的集數會跳過，
只有標記 `truncated` 的會被新來源取代。

### `pending`：登記了，但還沒做完

`inventory.json` 的正本在 `Kari-SRT/`（spec 說主 repo 只放程式／測試／文件）。
但登記必須發生在校讀之前——`batches.py`、`ingest.py` 都要靠它查每集的
`srt_name`。所以 `add_episodes.py` 寫進去的集數帶 `"pending": true`：

- `rebuild --verify` **跳過** pending 集數，不要求它們的 cues／TSV／SRT
- `smkul.csv` 的交付版也**不列** pending 集數
- `publish.py` 整批把關通過後才清掉旗標

於是批次做到一半，store 仍然自洽，`rebuild --verify` 全程可以是綠的。三個
標記語意不重疊：`truncated`（來源不完整，永不交付）、`partial`（已交付但
來源短）、`pending`（本批還在做）。

### 兩份 `smkul.csv`

`make_all.py` 寫 `kithann/out/smkul.csv`，列**全部**集數含還沒做完的，隨時可
刷新，是給人看進度的。`publish.py` 寫 `Kari-SRT/news/smkul.csv`，只列已交付
的，而且整批做完才寫。分開的理由是可重建性：「待處理（尚未切cue）」這種狀態
只存在 work dir，而 `rebuild` 沒有 work dir，重建不出來——交付版裡若有這種
列，就永遠對不起來了。

每一步都可中斷重跑：`run_cues.sh` 跳過已有 `cues.json` 的 work dir，
`fetch_sftp.sh` 也是，`gap_sheets.py` 拒絕覆蓋已校讀的 work dir，
`verified.json` 記錄哪些 sheet 讀過。這在實務上救過兩次——一次 devcontainer
重建、一次 session 中斷，都是直接接著跑完。

## 影片來源：SFTP

完整語料在 `ilrdf-corpus@192.168.35.10:/docker/ilrdf-corpus`，本機 `kithann/`
底下那 24 支 mxf 只是其中 `族語新聞/110.1-110.10/2月原始mxf檔` 一個月。

```
族語新聞/110.1-110.10/{1月…10月, 2月原始mxf檔}
族語新聞/111.1-111.5/{1月…5月}
族語節目/開會了/
110年2月_族語新聞文稿/          # 文稿，本機已有
```

實測：一個月 71 集、mp4 共 154 GB（平均 2.17 GB）；15 個月推估
**約 1,065 支、2.3 TB**。下載實測 **75 MB/s**，比本機那顆 NTFS-FUSE
外接碟（42 MB/s）還快，所以從 SFTP 抓比從本機碟讀更划算。

檔名跟 `ilrdf-corpus.csv` 的「影片檔案位置」欄一一對應，整批可以直接由
那份目錄驅動，不必再爬遠端。

### 連線

密碼在 `.sftp-pass`（已 gitignore），用 `scripts/news/sftp.sh` 包起來：

```bash
scripts/news/sftp.sh ls /docker/ilrdf-corpus
scripts/news/sftp.sh get '/docker/…/魯凱語-霧台20210101S1100.mp4' /tmp/x.mp4
```

密碼從頭到尾只以**檔案**存在，不進指令列、不進 `ps`、不進對話紀錄
（規定見 `CLAUDE.md`）。兩個卡很久的坑：

- **`sftp -b` 會自動開 `BatchMode=yes`**，而 BatchMode 會**停用密碼提示**。
  ssh 於是連問都沒問就回 `Permission denied (publickey,password)` —— 這個
  錯誤訊息跟「密碼打錯」長得一模一樣，但根本不是。要明寫 `-o BatchMode=no`。
- **`sshpass` 餵不到 `sftp`**。sftp 另外 fork 一個 ssh 子行程，密碼提示走
  `/dev/tty`，sshpass 包在父行程外面的 pty 傳不到孫行程。同一組密碼用
  `ssh` 直接連是會過的，換成 `sftp` 就失敗。改用 OpenSSH 自己的
  `SSH_ASKPASS` ＋ `SSH_ASKPASS_REQUIRE=force`，任何深度都有效。

### 抓檔與刪檔

沿用 `run_cues.sh` 的模式，只是把「從慢碟複製」換成「從 SFTP 下載」：

```
逐集：下載 → 比對位元組數 → cues（切 cue＋出 sheet）→ 刪影片 → 下一集
```

**刪除時機**：影片只有 `cues` 這一步需要。之後視覺辨識讀 `strips/`、
`sheets/`，SRT 組裝讀 `cues.json`，都不再碰影片。所以 `cues.json` 一寫出來
就可以刪，磁碟峰值 = 1 支影片 + 累積的 work dir（實測約 0.5 GB／集）。

**下載完一定要驗位元組數**。本機那批 24 個檔就有 2 個上傳不完整，
`ffprobe` 的 duration 照樣宣稱完整（見下）。遠端 `ls -l` 的 size 對本機
size，不符就重抓、不進解碼。

**誰要下載，由 `plan_month --todo` 判，不是 shell 自己判。** 規則只有
一條：**登記了（`pending`）而且還沒有時間軸的才抓。**

影片的最後一個用途就是切 cue——之後視覺辨識讀 `strips/`、`sheets/`，SRT
從 `cues.json` 組。所以時間軸只要存在，那支影片就沒有人要了。

2026-08-31 兩條舊規則併成這一條（使用者裁定）：

- 原本 `pending` 是**無條件**重抓。先前 `--limit 2` 試跑抓的兩支其實
  8/21 就切好了，下載 2.7 GB 只為了驗一次帶位再刪掉。
- 原本已交付但本機沒影片的也抓，用意是先把原生格備著給重讀用。重讀自己
  有抓檔的路（要用才抓、做完就刪），整月抓檔不必替它
  先囤。

於是清單上不會再有已經切過的集數，這條路徑也就**沒有辦法**去重切任何一
集——那正是最危險的失手：cue 整排重編號，交付的 SRT 時間全部對不上，而且
沒有一個地方會報錯。

**「切過了沒」要問兩個地方**（`plan_month.already_cut`）：

1. 本機 work dir——`<slug>.work` 和 `<slug>.B.work` 任一個有 `cues.json`
   就算（054–059 那批只有後者）。這份判斷 `make_all` 也要用，所以收在
   `paths.has_cues`，只留一份。
2. **store**：`1-cues/<月份>/<srt_name>.json`，交付時間軸的正本。
   `kithann/` 是 gitignore 的，devcontainer 重建就整個沒了（發生過兩次）
   ——只問 work dir 的話，那時候每一集已交付的看起來都「還沒切」。

### 版型由「資料夾」決定，不是檔名

`presets.json` 原本只能拿**檔名子字串**比對，但這批語料的檔名不可信：同一個
節目會叫 `魯凱語-霧台20210101S1100.mp4`、`賽德克-20210102s1100.mp4`、
`排灣-20210102s1800.mp4`，沒有共同片段；硬找一個又可能誤中「開會了」那批
版型完全不同的檔案，套錯 region 會整批切在錯的地方而且**不會報錯**。

所以 `subs2srt.py cues` 加了 `--preset NAME`，由呼叫端指定：

```bash
subs2srt.py cues VIDEO -o WORK --preset titv-news    # 族語新聞/ 底下都用這個
```

判斷依據是**檔案在哪個資料夾**（`族語新聞/` → `titv-news`；
`族語節目/開會了/` → `aiyalaeho-bilingual`，在 `scripts/aiyalaeho/presets.json`），
這比檔名可靠。
preset 名稱打錯會直接中止並列出可用的名稱。

### 開跑前用 verify_band.py 對照畫面

`subs2srt.py detect` 在這批素材上**不能單獨採信**——它會把氣象圖卡和台標
排在對白字幕前面。`verify_band.py` 改成量測 + 比對：抓幾分鐘的列剖面，
確認兩個地標，不合就中止。

```bash
python3 -m scripts.news.verify_band VIDEO --preset titv-news    # 加 --quiet 只看結論
```

- **對白平台**要落在 preset 的 region 內，否則就是讀錯地方了
- **紅帶上緣**如果看得到，不可以落在 region **裡面**，否則標題與受訪者名條
  會被當成對白切進去

紅帶在 region **下方**多遠都沒關係：2 月母帶是 y=848（距 region 底 4 px），
1 月一支卑南是 y=917，兩個都安全。`fetch_sftp.sh` 每個資料夾自動跑一次。

**兩個地標要用「形狀」分，不能用「高度」分。** 一行字幕是**寬**的特徵——
五十幾列差不多的墨，字身那麼高；圖卡邊框是**細**的——兩三列近白，上下都是
普通畫面。原本兩個地標都取「最亮的列」，在 7月 那批 mp4 上連錯兩次，兩次
都是把好檔案擋掉：

| 檔案 | 原本判成 | 實際 |
|---|---|---|
| `21NL003_41午間` | 「紅帶上緣 y=835 落在 region 裡」→ 中止 | 這集**根本沒有紅帶**，y=835 是字身最下面那一列 |
| `21NL004_37晚間` | 「對白平台 y=847 落在 region 外」→ 中止 | y=847 是紅帶邊框，只差 region 底 3 px；真正的對白平台在 y=800 |

改成：對白平台取**平滑過後**（視窗 24 列，約一個字高）最高的列，細線會被
抹平；紅帶上緣取**平滑丟掉的那部分**最高的列，而且要比它**自己附近的底**高
2 倍以上才算數。跟自己的底比而不是跟對白比，紅帶只出現半場也還讀得出來
（分子分母一起縮），四個實測案例分得很開：

| 檔案 | preset | 對白平台 | 細線 | 細線/底 | 結果 |
|---|---|---|---|---|---|
| 魯凱語-霧台20210101S1100 | titv-news | y=814 | y=849 | **3.12** | PASS |
| 魯凱語-霧台20210101S1100 | amis…（故意套錯）| y=848 | y=836 | 1.01 | FAIL（平台落在 876..1014 外）|
| 21NL003_41午間 | titv-news | y=807 | y=835 | 1.23 | PASS（無紅帶）|
| 21NL004_37晚間 | titv-news | y=800 | y=919 | **4.50** | PASS |
| 卑南語-20210103S1800 | titv-news | y=805 | y=917 | **6.40** | PASS |

門檻設 2.0。套錯 preset 照樣被平台那條擋下來，這點沒有變鬆。

#### 門檻 2.0 是幾集校的：本機 63 支存檔 mkv 全量重量過

上面那張表是 **4 支**校的，而且是在 `BUG_MARGIN`（量測略過左邊 480 px
台標）之前量的——量法改過就不能沿用舊校準。所以拿本機 63 支存檔 mkv
（2 月整批）用**現行量法**重量一遍，一支約 41 秒牆鐘、4 分鐘 CPU，
`nice` 循序跑。

    for f in kithann/out/mkv/*.mkv; do
        nice -n 15 python3 -m scripts.news.verify_band "$f" --preset titv-news
    done

結果（63 支，比值由細到大）：

| 群 | 支數 | 比值範圍 | 紅帶上緣 y |
|---|---|---|---|
| 判「有紅帶」 | 17 | 2.08 – 5.35 | 847–850（16 支），753（1 支）|
| 判「無紅帶」 | 46 | 0.63 – 1.84 | — |

**兩群無相疊**：1.84 佮 2.08 中間空 0.24，門檻 2.0 就khiā佇彼个空縫
內底，[1.85, 2.07] 內底隨在你揀，63 支ê判定攏仝款。所以門檻**免改**。

毋過愛講清楚：**這毋是清清楚楚ê雙峰**。低彼群 46 支khiūnn佇 0.63–1.84
（平均相隔 0.03），懸彼群 17 支散佇 2.08–5.35（平均相隔 0.2），所以懸
彼群內底ê空縫（0.24、0.26、0.27、0.45、1.07）比門檻遐个空縫閣較大。
門檻khiā佇遐是「無一支落佇附近」，毋是「兩群天生分做兩爿」。

紅帶上緣ê **y 值**顛倒是較穩ê結構證據：17 支內底 16 支攏佇 y=847–850，
賰彼一支 y=753 就是下底講ê假警報。

#### 63 支內底一支 FAIL，是假警報

`20210225_056_午間_Truku_太魯閣`：細線判佇 y=753，落佇 region
（722–844）內底，所以判 FAIL、批次會停。抽原生格看過——**y=750 是
「資料畫面」彼个標籤ê墨，毋是紅帶邊**；真正ê對白（「除了政治經歷」）
佇 y≈800–850，好好仔佇 region 內底。

所以這支ê版型是好ê，是判準看毋著。失敗ê方向是**擋著好檔案**（安全
彼爿），毋是放過歹ê。意思是：region 內底任何一條光ê幼線——資料畫面
標籤、圖卡邊——攏會予當做紅帶邊kā批次擋落來。拄著ê時抽格看一下就
好，毋免改門檻。

### mp4 的字幕帶跟 mxf 一樣，版型卻不只一種

mp4 是 1920×1080 h264，沒有 soft subtitle。region 一樣是
`[0, 722, 1920, 122]`，四個實測檔的對白平台都落在裡面。

但**同一個資料夾裡的版型不保證一致**。`110.1-110.10/7月/` 底下至少兩種：

- **完整新聞版型**：紅帶在 y=846 與 y=919 兩道，對白在 region 內（037晚 排灣）
- **無紅帶版型**：整場只有左下角一塊族語標章（在 region 下方），對白直接壓在
  畫面上（041午 鄒，春節特別節目）

所以 `fetch_sftp.sh` 那句「每個資料夾驗一次」只是省時間，不是保證；換新資料夾
第一支請不加 `--quiet` 看剖面，再開 `sheets/sheet_001.png` 確認圖條上只有對白。

（版型怎麼選見上面「版型由資料夾決定」一節。）

## 幾個踩過的坑

**語料掛載點很慢。** `/dev/sda1` 是 NTFS-over-FUSE，實測單串流 41 MB/s、
雙串流 21+21 MB/s，也就是整顆碟就這樣。同時開三個解碼反而各自掉到
11.6 MB/s（慢 3.5 倍）。所以 `run_cues.sh` 改成：一次只循序複製一個檔到本機
磁碟（寫入 822 MB/s），解碼吃本機那份，解完就刪。380 GB 的讀取約 2.5 小時，
這是硬底線，解碼藏在它後面跑。

**檔名不可信，要用畫面驗證。** 五個 2021 年 2 月的檔名年份打成 `20`，
`21NL004_37午間` 掛的是晚間的 NL 代碼卻是午間。集數其實是「當年第幾天」
（集 32 ＝ 2 月 1 日），所以用「集數＋時段」查 `ilrdf-corpus.csv` 就夠。
時段取檔名裡的中文字，不取 NL 代碼；再把畫面左下角燒死的族語標章
（泰雅／撒奇萊雅／Seediq…）截出來跟目錄對照，24 個檔全部相符。

**上傳不完整的檔案。** 完整母帶是固定 6.30 MB/s 的 CBR。有兩個檔明顯偏低
（2.83 與 3.44 MB/s），檔頭仍宣稱完整長度，但尾端解碼毀損 —— 這兩集當初
跳過（041午 鄒、037晚 排灣）。只看 `ffprobe` 的 duration 看不出來，要拿
位元組數除以長度才會現形。

**「上傳不完整」要分清楚是這一份壞，還是這集只有這麼多。** 後來拿
`110.1-110.10/7月/` 的 mp4 版本重抓這兩集，結果分岔：

- **037晚 排灣救回來了**：mp4 完整 49:20，2 月那份 mxf 只是壞掉的複本。
- **041午 鄒沒救**：mp4 只有 11:13，最後一句講到一半、末幀破圖，跟 mxf 那份
  「約完整檔 45%」完全吻合——伺服器上兩個位置是同一份短檔。

所以「來源短缺」跟「這一份抓壞了」是兩件事：前者換來源沒用，後者換就好。
041午 鄒照樣做成 SRT（有總比沒有好），inventory 多一個 `partial` 欄，
`make_all.tracker_row()` 把它接在狀態後面 —— 掛在 `tracker_row()` 而不是
產 SRT 的地方，`rebuild.py` 才不會跟 `make_all.py` 講不一樣的話。

**`fetch_sftp.sh` 只做一集就說整批做完。** 迴圈原本是
`while read ... done < "$listing"`，清單掛在 stdin；迴圈裡的 `ssh`（下載）
跟 `ffmpeg`（切 cue）都會把 stdin 吸乾，剩下的集數整批消失，然後印
`finished: 1 episode(s) cut` 收工——**看起來完全正常**。這支從加進來就只用
`--limit 1`／`--limit 2` 試跑過，限流本來就只做一兩集，所以一直沒現形；
第一次真的拿去跑 13 集才炸出來。清單改掛 fd 3（`read <&3` ＋
`done 3< "$listing"`），子行程愛吸 stdin 隨便它。

**母帶開頭的 slate 會被當成字幕。** 播出母帶開頭是彩條與識別卡，識別卡的
白字剛好落在字幕帶範圍內，segmenter 會在第 0 幀開一個十幾秒的 cue。
2 月那 22 集裡最早的真實字幕出現在 5.8 秒，所以 `make_srt.py` 直接丟掉
「start < 0.5 秒」的 cue。

## 切 cue 失效時的補救：blind_cues → 視覺辨識重讀

切 cue 是逐格比對遮罩（機制與失效模式寫在
`.claude/skills/video-subtitle-srt/SKILL.md` 的 Cue segmentation）。它有兩種
失效，都會把好幾句字幕併成一格，而且**圖條看起來完全正常**——中位數把
其他句洗掉，讀者照抄，沒有任何地方會報錯：

| 失效 | 成因 | 特徵 | 判準抓得到嗎 |
|---|---|---|---|
| 卡死 | 背景會動（碎石、水面、白衣），遮罩逐格全變，兩道閘門同時失效 | `frames` 極低、時長很長 | `blind_cues` 的差額判準 |
| 靜態亮背景 | 白紙、雪地這種**不動**的亮背景灌爆遮罩，字幕只佔 7% 以下，換句看不出來 | `frames` 正常、時長很長 | **抓不到**，只能靠時長 |

所以觸發條件是**兩個**：`blind_cues` 的差額 ≥1 秒，**或**時長 ≥6 秒。實測
（59 集、53,582 條交付 cue）：

```
差額型      4,440 條，未見 16,036 秒
時長 ≥6 秒  3,252 條，其中差額判準抓不到的 2,125 條，合計 17,664 秒
```

第二型比第一型還多，漏掉它等於漏掉一半。

**這兩道閘門是幾集校的、召回率偌濟。** `LONG = 6.0` 是照
20210222_053 彼幾條校ê。後來 a9 提《開會了》083 量召回：25 條有實據
ê漏切，兩道閘門**干焦掠著 12 條**。漏去彼 13 條分做兩類，兩類**結構
頂懸**就掠袂著，毋是門檻調一下就好ê：

- **短ê騎線 cue**（0.8–1.3 秒）：cue 比一句字幕閣較短、騎佇換句彼
  搭。`frames` 罩甲滇滇，差額≈0；閣傷短，`LONG` 嘛無響。
- **5 秒藏 3 句**：差額嘛是 ≈0，時長拄好落佇 `LONG` 下跤——`LONG`
  giú落去到掠會著遐，規集大部份ê cue 攏綴leh入來矣。

第三个判準 `spread`（一條 cue 內底逐格遮罩對頭一格ê上大距離）試過，
**無成**：配收狹ê遮罩（`max_spread` 20）掠會著 22/25，毋過誤報
**38/60（63%）**，讀者實地掃出來ê真比例才 8–10%。三擺內底毋著兩擺
ê毋是閘門。所以 `spread` 會使排順序，袂使做判準。

兩个門檻攏是 CLI 參數（`--floor`、`--long`），換一齣新ê節目是**重量**，
毋是重臆。

補救走**既有的視覺辨識**，不改切 cue：依 `blind_cues` 的清單，從 mkv 抽
5 fps，用 0.5–0.6 秒的時間中位數合成遮罩再兩兩算 Jaccard 分段（實測真的
換句 ≥0.68、雜訊 ≤0.33，分得很開），一段出一張圖條，走既有的批次。邊界
要精修就交給 `refine_cues`——它的設計前提正是「兩側身分已知」。

**改 TSV 的判準**（踩過兩次）：

- 比對要用**整格相等**，不是子字串。`grep -F "老人家"` 會被別處那句
  「老人家就會跟我們講說」命中而擋下，但那是不同時間的另一行。誤擋的
  正好是短句，而短句本來就是中位數最容易洗掉的。
- 「鄰居已承接」**只能擋補字，不能擋改寫**。一格裝著鄰居的句子就是錯的，
  不管鄰居寫什麼。一句字幕跨好幾個 cue 是常態——全批 12,351 處相鄰
  cue 文字完全相同——所以改寫造成的「重複」是這個語料的正常寫法。

**補文字不等於修好。** 時間軸沒動，14 句會串成一條 27.58 秒的字幕，
對閱讀是進步，對訓練語音辨識不見得——音檔和文字對不起來。全批這種
「≥10 秒且 ≥40 字」的條目有 22 條，要真正修好得回頭重切那幾段 cue。

## 集數ê短寫按怎還原做檔名（追蹤用）

這份文件內底ê `055午`、`058晚`、`037晨` 這款短寫，是「集數編號＋時段」。
**莫閣去產對照表**——正本本底就有兩份，加一份就加一份會過時ê物件：

- `Kari-SRT/news/smkul.csv`：集數、播出日期、播出時段、族語別、影片位置、
  字幕狀態。查 `055午` 就是揣「集數 55、時段 午間」彼逝。
- `Kari-SRT/ilrdf-corpus.csv`：閣有 `srt_name` 彼欄，就是檔名。

檔名揣著了後，欲揣ê物件佇：

| 物件 | 路徑 |
|---|---|
| 圖條佮 strips | `kithann/out/mxf/<slug>.B.work/` |
| 視覺辨識 TSV | `Kari-SRT/news/1-ocr/2-vision/<月份>/<srt_name>/b*.tsv` |
| 交付ê SRT | `Kari-SRT/news/1-ocr/3-srt/<月份>/<srt_name>.srt` |
| Cue 時間 | `Kari-SRT/news/1-ocr/1-cues/<月份>/<srt_name>.json` |

## 重讀ê判讀判準（重讀全批 49 集實測積起來ê）

彼 49 集是 `Kari-SRT/news/1-ocr/3-srt/2021-02/` 內底 032 到 053 這區間（`20210201_032_午間_Atayal_泰雅` … `20210222_053_晚間_Amis_阿美`）。下底逐條攏有講是佇佗一集踏著ê，愛追就對彼个檔名去揣。

重讀ê圖條是**該段影格ê逐像素中位數**，佮頭一擺辨識ê圖條仝款會騙人，
干焦尺度較細。下底逐條攏是佇具體案例頂懸踏出來ê，順序就是判定順序。

### 一、圖條頂懸ê「疊影」，濟數毋是疊印

看著兩句糊做伙，第一反應是「交叉溶接、留空」。**毋著**——逐格看，
濟數是**先後出現**（切點硩佇句中），彼款愛判予佔時間較濟彼句，
袂使留空。真正同時上畫面（淡入淡出交疊）才留空。

重讀頭前彼幾集我一律當做疊印留空，等於白白擲掉一批讀會出來ê字。
（改判了後掠著ê實例：`20210206_037_午間_Seediq_賽德克` 段 1044.08、`20210221_052_午間_Kanakanavu_卡那卡那富` ê 23 个相黏 cue。彼幾集是佇使用者叫我記 token 進前做ê，所以無逐集ê紀錄——`kithann/out/token-log.tsv` 是對 037 才開始記ê。）

### 二、判定順序

```
先後出現            → 判予多數句
段內夾空白、三方無過半 → 判予**相對多數**（按呢救轉來一个單字「嗯」，
                      彼是伊唯一园會落ê段）
差 <0.05 秒         → 照多數判，毋過愛列出來予人覆核
完全平手            → 看**佗一句會無去**：
                      兩句佇別段攏無著落 → 兩句攏寫，直接相接
                      一句佇厝邊已經是多數 → 讓予無著落彼句
                      兩句攏有著落 → 隨在提一句
某句佇逐段攏毋是多數  → 若某段ê多數句佇厝邊嘛是多數，就kā彼段讓予
                      予人夾牢彼句（**重新分配，毋是放低標準**；
                      按呢救轉來「八八風災之後」）
```

**「差 <0.05 秒」報了猶原愛判；對稱ê時倒轉去相對多數。**
20210206_037午 段 1044.08（2585.10–2585.90）兩句差 0.0008 秒，逐格掠
著ê換句點 2585.4996 拄好佇該段ê中央——揀佗一句，切出來ê cue 界線攏
偏 0.40 秒，時間軸頂懸無優劣。這款無別个憑據ê時，就照相對多數判
（該段是「就是1個月的時間」贏 0.0008 秒）。閣愛先確認兩句佇厝邊ê段
攏有厝，無人會因為這个判定規句無去。

### 三、抽格ê方法會改變答案

**愛用 `-copyts` 配 `select='between(t,…)'` 抽真實 PTS ê原生格**，
閣採「PTS 是 T 彼格涵蓋 [T, T+1/29.97)」算佔比。

`-ss` 概略 seek 佇 5 fps 之下有 ±1 格誤差，已經**判反過 13 條**。有一
條（20210218_049 段 325.03）佇圖條佮概略抽格之下攏會判反，干焦逐格
原生 PTS 才翻會過來。

**一段內若有兩擺切換，圖條就無通信。**

抽格ê時閣有兩个 ffmpeg ê坑（20210206_037午 讀者踏著ê）：

- **`-nostdin` 無加ê時，ffmpeg 會kā `while read` ê輸入食去**，紲落來
  逐段對著ê時間就全走位。佇迴圈內底叫 ffmpeg 一定愛加。
- **`-ss` 若無配 `-t`，`select` 篩了後 ffmpeg 猶原解碼到檔尾。**
  加 `-t` 了後逐段對 22 秒降到 1.8 秒（12 倍）。

**`-frame_pts 1` 出來ê號碼是「格號」，毋是毫秒。** 秒數愛家己算：
`格號 × 1001 / 30000`。

**`between(t, …)` ê秒數愛先四捨五入到毫秒。** mkv ê時間戳是 1 ms ê
網格；直接提 `n*1001/30000` 這款無量化ê秒數去 select，**會恬恬漏掉
將近一半ê格**——ffmpeg 回 0 毋過無報錯。用
整數算：`ms = (2*n*1001 + 30) // 60`（＝ `floor(n*1001/30 + 0.5)`），
才逐格中。**`round(n*1001/30000, 3)` 猶原無夠**——Python ê `round()`
是五成雙，muxer 是四捨五入，拄好 .5 ê格會差 1 ms；量過頭 3000 格
內底有 55 格按呢（間隔 60/30 相間，1.8%），20210205_036晚 補做彼支
就是佇 segment 194 掛掉（格數 23 對袂著 24）。（20210204_035午 讀者
229 格內底本底才中一半。）（mkv 容器ê 時基是 1/1000，真簡單掠做毫秒；
2026-08-29 kā 20210206_037 cue 498 ê對照圖規排標毋著，差 42 倍，
好佳哉圖頂懸ê字家己講話。）

### 三之二、一擺換句ê時，抽中點彼格就夠——**三个狀態就無效**

一段內若干焦換一擺句，**涵蓋該段中點彼格所顯示ê句，定義上就是佔
多數彼句**。免逐格算，抽一格就定案。20210202_033 用這步覆核 77 段
（54 个多段 cue），改著 7 段。

用進前先驗：`-ss` 起點提 3／6／12／25 秒攏著得著**逐位元組相仝**ê
PTS 佮畫面，按呢才確認無概略 seek 誤差。

**三个狀態以上，中點格佮多數法會相反。** 20210207_038晨 段 830.05：
燻乾 0.23 秒 → 無字 0.20 秒 → 磨成粉 0.37 秒——中點彼格是空白，
相對多數是「磨成粉」。中點格干焦是頭一步ê定案；掠著換句超過一擺
就一定愛倒轉去算秒數。

### 三之三、家己揣邊界ê程式有兩个會判反ê所在

用「比對佗一爿較相似」去自動揣換句點，下面兩種情形會kā邊界推晏，
攏著靠中點原生格擋落來：

| 情形 | 實例 |
|---|---|
| 邊界一爿是無字幕、背景閣咧振動 | 20210202_033 cue 858 推晏 0.47 秒，險仔kā 858.02 對「空白」翻做有字 |
| 換句附近有鏡頭切換 | 仝集 cue 959 推晏 0.63 秒，險仔kā 959.02 翻掉 |

仝集 33.02、663.06、956.03、957.05、897.02 自動流程一度欲改，中點
原生格證實**本底ê空白／原字才著**。

### 四、三種漏法（攏實測過）

| 漏法 | 實例 |
|---|---|
| 短字幕予中位數洗掉 | 「搶搶搶」三字、「12號」0.4 秒，圖條頂懸全然看無（中位數 ink ＝ 0，逐格 ink 986–7182） |
| 兩句字形傷相像 | 「北部8至23度」→「中部9至26度」遮罩差異才 0.117，遠低過門檻。氣象、票價、日期時段這款版型固定干焦數字變ê愛特別注意；中位數閣會kā兩句糊做錯字（實測「容易有雨」其實是「容易下雨」） |
| 亮背景假陽性 | 亮蕨葉、亮衫、報紙、路面、亮岩石、溪水、亮人行道、石雕牆，ink 2300–4900 煞無字。純亮度遮罩無通信，愛用「白芯＋四箍圍仔黑描邊」偵測 |

### 四之二、重讀ê切段器嘛會著仝一款漏法

頂懸彼三種漏法**毋是干焦交付管線ê問題**——`reread` ê切段器行ê是仝
一路，所以仝款會著。上歹ê組合是第二種（兩句字形傷相像）：

```
20210219_050 cue 1115（11.1 秒）
  畫面 2753.8–2760.2  今日氣溫北部8至23度
       2761.0–2764.2  中部9至26度
  既有        今日氣溫北部8至23度        ← 干焦頭一句
  重讀切段     一段                    ← 亮氣象圖，遮罩變無到 7%
  讀者        今日氣溫北部8至23度中部9至26度  ← 兩句攏看著，煞干焦一格通寫
  safe_resplit  跳過（單段，免動）        ← 讀者揣著ê彼句規句無去
```

仝一集 cue 1116（南部10至28度／東部10至25度）仝款。

**後果是靜恬恬ê。** 切段器失敗ê時，讀者ê發現顛倒予人擲捒，而且無一
个所在會講。所以 `safe_resplit` 這馬會另外報一逝「切袂開、毋過讀者
讀著無仝ê」（`resplit.unsplit_but_changed`）。**伊干焦報，無家己改**
——讀者讀毋著字（「邊波」／「邊坡」、「彌猴」／「獼猴」）佮讀者看
著較濟句，佇資料頂懸生做仝款。

量過ê規模：十七集讀著 3,266 條相異句，尾仔無入正本ê 22 條（0.7%）。
彼 22 條內底約略：4 條是讀者讀毋著字（把關擋著）、2 組是讀者kā既有
彼句切碎（仝款擋著）、6 條是真正掠會轉來ê——4 條是頂懸這款切袂開ê
（036午 2 條、050午 2 條），2 條是規句厝邊攏無ê（049午「自己掏金錢」、
051晚「讓他使用它」）。

### 四之二之二、亮背景ê時，XOR 分會開，Jaccard 分袂開

第二種漏法（兩句字形傷相像）佮第三種（亮背景）害ê攏是仝一件代誌：
**相似度ê分母予背景灌大**。`cuelib.mask_distance` 是 Jaccard，除ê是
聯集——亮報紙、亮氣象圖ê時聯集偌大，兩句無仝ê彼點仔差就予稀釋去，
20210201_032午 cue 17 ê距離才 0.346，掉佇 0.35 門檻下。

**改算相鄰兩格遮罩ê XOR 絕對像素數就分會開。** 無振動ê背景對 XOR
ê貢獻是零，換句ê時會走出一个尖峰。20210201_032午 讀者實測：

| 做法 | 漏去ê |
|---|---|
| 對中點參考格ê涵蓋率 | cue 982 |
| `mask_distance`（Jaccard） | cue 17（0.346）、cue 1061 |
| **相鄰格 XOR 絕對像素數** | **無** |

（另外一位讀者仝一工用「參考格召回率」`|ref ∩ frame| / |ref|`——
分母是參考格，無是聯集——嘛掠著 20210201_032午 全部 24 段。兩款ê
共同點是**分母莫予背景牽連**。）

**毋過召回率家己有兩个盲點**（20210204_035晚 讀者揣著ê）：

1. **對「加字」是盲ê**——新字若是舊字ê超集，召回率顛倒 1.0。愛
   配「殘差絕對像素數 ≥1200」。
2. **亮閣靜止ê背景猶原會失效**——背景佔遮罩絕大部份ê時，兩句無仝
   ê四字句召回率會到 0.79（cue 1150，攝影棚白底）。彼陣愛逐格減去
   段內 88% 以上攏著ê**靜止遮罩**，賰ê殘差才分群，閣要求換句是
   **單向ê**（標籤袂越轉來）。按呢才掠著彼段。

所以無一款判準家己會用得；**上尾猶原愛抽原生格目睭看**。

這是講**切段器彼爿有法度改**，毋是干焦靠讀者補救。猶未做。

### 四之六、既有嘛有「相黏」ê——毋是干焦漏，是**糊做一逝**

20210221_052午 揣著另外一款：既有 OCR 讀ê是**中位數合成圖**，兩三句
疊做伙ê時，伊kā合成ê字串當做一句記落去。`20210221_052_午間_Kanakanavu_卡那卡那富`
34 个多句 cue 內底
**23 个是這款**（比論 cue 589 記做「在做族群文化的工作其實時間」）。

這佮「漏字」無仝：字**有**佇正本，毋過是**糊做一逝**，時間軸嘛毋著。
`20210221_052_午間_Kanakanavu_卡那卡那富` 規集重讀了後，真正規句查無ê
干焦 1 句——賰ê 32 句攏藏佇相黏ê
字串內底。

意思是：**「既有查無ê句」這个數字會低估**。愛閣看「既有這條 cue ê
文字敢是幾若句相黏ê」。切開了後時間軸才會著。

### 四之七、既有資料ê三款失敗，愛分清楚

| 款 | 情形 | 實例 |
|---|---|---|
| **漏** | 一條 cue 有幾若句，既有干焦記一句 | 規批到處攏是 |
| **相黏** | 中位數合成圖kā兩三句疊做一串，既有照抄 | 20210221_052午 34 條有 23 條 |
| **沿用厝邊** | 這條 cue ê文字是**厝邊彼條**ê句，家己彼句無記 | 20210222_053晚 cue 803 沿用 801、cue 825 沿用 823/824 |

三款ê修法無仝：**漏**切開就好；**相黏**切開閣愛重分字；**沿用厝邊**
是既有彼格家己就毋著，切開嘛袂當家己好。報告愛分開講。

### 三種會用ê分界判準（無仝集適合無仝款）

到 tann 讀者實測有效ê有三款，攏是「莫予背景牽連」ê變體：

| 判準 | 適合 | 失敗ê時 |
|---|---|---|
| 相鄰格 XOR **絕對**像素數 | 靜止背景 | 背景會振動ê時通通報警 |
| 對參考格ê**召回率** | 一般 | 對「加字」是盲ê；亮靜止背景猶原失效 |
| 相鄰格遮罩ê**交集**像素數 `\|mask_i ∩ mask_{i+1}\|` | **背景會振動** | — |

第三款是 20210221_052午 揣著ê：彼集背景振動傷厲害，XOR 佇 244 段
內底有 204 段報警，無路用；交集ê話，仝一句是字形本身（5,000–20,000
px），換句就掉到 1,000–3,000，用「低過該段 P75 ê 0.70」做門檻，83
个嫌疑點逐點看，20 个是真ê換句、其餘是鏡頭切換。

**無一款會使家己用**；上尾攏愛抽原生格目睭看。

### 四之四之二、`reread` ê段無罩牢規條 cue（7.1%）

量過：45 集、5,214 條有重讀ê cue，**370 條（7.1%）ê段佮段中間有空縫**
——`reread` 跳過彼條 cue ê一部份。（這是算「仝一條 cue ê段之間」ê
空縫；段若是起傷慢抑是煞傷早，這个量法掠無，所以是**低估**。）

**代價毋是「彼幾秒無讀著」爾爾。** 既有ê句若拄好落佇空縫，讀者ê段
內底就揣無伊，把關就會kā**規條 cue** ê切分擋落來——一句都救袂著。
20210211_042午 cue 69 就是按呢：畫面 6 句、既有記ê「並不是一片平原」
落佇空縫，結果 4 句新句攏留佇外口。

（把關擋著是**著ê**——無彼句ê證據就袂使切。問題佇頂游：段愛罩牢。）

上濟ê幾集：046午 19/76、051午 16/157、037晚 16/116、052晚 14/208。

### 四之四、重讀家己嘛會切了無夠幼

`reread` kā一條 cue 切做幾若段，毋過**一段內底猶原有可能有三句**——
讀者一格干焦通寫一句，判予多數，賰ê兩句若別位無厝就無去矣。
20210204_035晚 頭 100 逝內底就有四段（780.04、890.03、636.03、
90.02），逐段ê絕對多數真明（1.1–2.8 秒 對 0.1–0.6 秒），毋過彼是
**這條線家己ê殘留漏空**，毋是既有ê問題。

處理法佮「規段切袂開」仝款：先看賰ê彼句佇別段抑是厝邊 cue 有厝無，
無厝才相黏寫。相黏歹看，毋過規句無去是袂補ê。

### 四之五之二、字幕帶ê位置**無khiā佇正所在**（48 集量過）

**量法：「有字ê cue」ê逐列 ink 剖面，減「空白ê cue」ê剖面。** 靜止
ê下三分之一圖形兩爿攏有，會家己相消；賰ê就是字幕。（頭一擺我用
「ink 上濟ê 122 列窗」量，量無準——揀著ê是下三分之一ê圖形。相減
這步是關鍵。）

20210208_039晚 ê差值剖面看會出兩个清清楚楚ê峰：

```
y 780–854    ← 字幕本體
y 930–1004   ← 「文化小辭典」超文字／下三分之一（本底就佇 crop 外）
```

48 集ê結果（揣ê範圍限佇 700–920，才袂予 930+ 彼个峰食去）：

| | 上細 | 四分之一 | **中位** | 四分之三 | 上大 |
|---|---|---|---|---|---|
| 起點 | 700 | 756 | **787** | 815 | 848 |
| 尾點 | 835 | — | **850** | — | 919 |

### 四之五之六、閣有**第三个**位置：專題段ê字幕落佇帶外，規段掠無

2026-08-30 佇 054–059 這批掠著ê，比頂懸講ê「兩个位」嚴重濟：
**規段專題（紀錄片式ê插播包）ê華語字幕印佇 y≈950–1035**，
`REGION` 是 `[0, 722, 1920, 122]`（y 722–844），**完全罩無著**。
Strip 是空ê、圖條是空ê、讀者無字通讀。

閣較歹ê是彼段ê**版型是顛倒ê**：受訪者名牌顛倒佇帶內，對白hőng
逼落去名牌下跤。所以標準帶掠著ê是名牌，毋是話。

#### 幾集、偌濟

用 vision TSV ê**長連紲空白**掠（離線就會使做，免看影片）：

| 集（完整檔名） | 上長連紲空白 | 舊 cue | 重切了ê cue |
|---|---|---|---|
| `20210225_056_晚間_Bunun_布農` | **147** | 147–293 | 147–342（196 條）|
| `20210224_055_午間_Cou_鄒` | **105** | 316–420 | 316–470（155 條）|
| `20210226_057_晚間_SaySiyat_賽夏` | **94** | 271–352 | 271–434（164 條）|
| `20210228_059_晚間_Pinuyumayan_卑南` | **93** | 12–104 | 12–148（137 條）|
| `20210228_059_午間_Kanakanavu_卡那卡那富` | **84** | 266–349 | 266–433（168 條）|
| `20210227_058_晚間_Paiwan_排灣` | **55** | 292–346 | 292–443（152 條）|

六集攏用 `rescan_band.py` 重切過矣（2026-08-31），重切ê紀錄留佇逐集
`cues.json` ê `rescanned` 欄位內底（`lo`／`hi`／`region`／`count`），
重切進前ê `sheets.json` 留做 `sheets.json.before-rescan`。

**五段攏是仝一个專題系列。** 湯守仁（055午）、洪成（056晚）、
陳賢德（057晚）、曾政男（058晚）、吳榮山（059晚），攏是白色恐怖／
二二八受難者ê紀錄片式專題，這五集拄好是 2 月 24 到 28 —— **二二八
進前彼禮拜**。所以這毋是四散ê缺陷，是某一个外製專題系列家己帶ê
版型，規批做伙入來。

意思是：**紀念日、節慶彼幾禮拜愛特別檢查**（外製專題上濟）；
閣有，36% 這个比例**袂使直接推去規語料**——彼是這禮拜ê比例。
已交付彼 60 集（2 月 1 到 22、1 月 1 到 5）一集都無，就是旁證。

056午 彼段 24 條（615–638）掃描量著 0 秒，讀者嘛講是實在ê空景
（純畫面／訪談過場），**毋是**這款病。

059晚 彼集ê讀者本底講「字幕整段無出現」——伊是看 strip 判ê。
**Strip 佮圖條攏是照字幕帶裁ê，字若佇帶外，in 兩个永遠是空ê**，
所以「strip 內底無字」證明袂著「畫面頂無字」。掠原生格用闊ê
crop（`crop=1920:360:0:690`）才看會著。這條已經寫入 brief。

**已交付彼 60 集無這款病。** in ê長空白段幾乎攏佇**節目尾**（片尾、
預告卡、台呼），時間戳攏貼牢 2880 秒收播；賰彼幾條中段ê（20–22 條）
抽格看過是實在ê空景（046午 611–631 秒是林地空鏡，帶內帶外攏無字）。

#### 按怎掠

**主判準是離線彼个**：vision TSV 內底連紲空白ê段（`blank_runs.py`）。
普通ê空隙（空景、圖卡）是幾條爾爾，94–147 條連紲就是版型問題。

**毋過長度家己分袂開，愛用證據。** 2021-01-06 ê 006 有一段**12 條**ê
字幕印佇帶**頂懸**（y ≲ 722，佮頂懸講ê下方 y≈950–1035 顛倒péng），
落佇任何長度門檻下跤；仝彼集上長ê連紲空白是**13 條**（1122–1134）
ê揭牌空鏡，是好ê，**比病灶較長**。用長度做唯一判準，這兩爿拄好
掠反。

分會開ê是**墨khǹg佇佗位**。字幕印佇帶頂懸，圖條干焦食著字ê下半
截，墨規陣khû佇上頂彼幾逝，下底是畫面；圖卡（資料畫面、報紙剪報）
是淺底烏字，`text_mask` kā規條攏當做墨。所以判準是兩爿做伙看：

    python3 -m scripts.news.blank_runs <srt_name>     # 逐集掠帶外字幕
    python3 -m scripts.news.blank_runs                # 規批ê長度總表

- **頂逝墨 ≥ 60**（`EDGE_INK`）：有夠濟才算字，毋是雜訊
- **頂逝墨佔規條ê墨 ≥ 0.11**（`EDGE_SHARE`）：夠集中才是予切ê字，
  毋是規面ê圖卡
- **痕 ≥ 10 條，中位闊 ≤ 12**（`EDGE_MARKS`／`MARK_WIDTH`）：**形**
  愛親像字——一逝字ê下半截是規排幼痕（一筆一痕）

圖條頂端算三逝（`EDGE_ROWS`），墨照 `cuelib.text_mask` 算——白字愛
有烏邊，所以白衫、雪地、族服ê白條紋攏袂算做墨。

第三條是後來才加ê，加ê理由是**頭兩條分袂開**：菜園ê鍍鋅水管
（002晚 cue 479）佔比 **0.309**，佮 006 彼段真ê（0.320）差不多仝
款，無一个門檻分會開。形就分會開矣：

| cue | 痕數 | 中位闊 | 佔比 | 實況 |
|---|---|---|---|---|
| 006 170 | 16 | 4 | 0.320 | 真：帶外對白 |
| 056晚 312 | 26 | 4 | 0.157 | 真：地名條 |
| 058晨 77 | 29 | 5 | 0.115 | 真：標題條 |
| 002晚 479 | 14 | **21** | 0.309 | 假：菜園水管 |
| 002午 737 | **6** | 3 | 0.237 | 假：布料掛飾 |
| 057午 386 | **2** | 3 | 0.030 | 假：過場動畫 |

真ê攏 ≥16 痕、幼；假ê毋是痕傷少就是痕傷闊。Spec 講ê本底就是這件：
「**字形**只剩下半截被截斷的證據」——量ê是形，毋是墨ê量。

**形這條嘛有一款過會去ê：族服ê織紋。** 手䘼頂ê織帶就是一排幼ê
光痕，佮一逝字ê筆畫痕仝形。002晨 cue 1（攝影棚，規張攏無字）
11 痕、闊 3，就按呢過關。這款大部份出佇**節目頭ê攝影棚鏡頭**，
時間彼欄看一下就知，這嘛是彼欄愛印ê另一个理由。

校準樣本佮實測（006，41 段空白）：

| 段 | 條數 | 頂逝墨 | 佔比 | 看過圖條ê實況 |
|---|---|---|---|---|
| 167–178 | 12 | 131 | **0.326** | 真ê：帶外字幕 |
| 1242–1251 | 10 | 292 | **0.158** | 真ê：記者名條印佇帶頂懸 |
| 690 | 1 | 97 | 0.065 | 假ê：桌面白邊配烏家私 |
| 274–278 | 5 | 34 | 0.048 | — |
| 149–150 | 2 | 387 | 0.017 | 規面ê報紙剪報 |
| 1122–1134 | 13 | 26 | 0.005 | 好ê：揭牌空鏡 |
| 191–196 | 6 | 0 | 0.000 | 資料畫面圖卡 |

0.11 是 006 內底「帶外有字」佮「無字」兩群ê中點（0.158 佮 0.065）。

**這支是複查清單，毋是閘門。** 掠著ê是「region 頂端有一逝字予切
斷」，這件伊掠了誠準；毋過**帶外有字有三種，攏是實在ê字**，圖條
內底無一項分會出來：

| 掠著ê物件 | 例 | 愛按怎 |
|---|---|---|
| 對白字幕印佇帶外，**讀者無看著** | 006 167–178（547–575 秒）| 重切／重讀 |
| 地名條、標題條印佇帶頂懸 | 056晚 312–316（850 秒）讀會出「…信義鄉受難公墓」；058晨 76–79（177 秒）是標題卡 | 看欲補無 |
| 節目尾ê片尾卡、預告卡 | 逐集攏有，時間 ≳2800 秒 | 正常ê，閬過 |

分ê法是**時鐘**，毋是墨：所以清單一定印時間，第三款一目就閬掉。

054–059 彼批（14 集，純負例）實測：**掠 9 段，節目尾 7 段、中段
2 段**，中段彼兩段（056晚 地名條、058晨 標題條）**攏是實在ê**，
誤報 0。形彼條判準加入去進前是 11 段、中段 4 段，內底兩段是誤報
（過場動畫、開場卡）——加了後拄好kā彼兩段擋掉，真ê一段都無落勾。

正例是 006 中段彼段佮 056晚 彼段，負例是 054–059 彼批賰ê（彼批本底
六段帶外ê病灶已經用 `rescan_band.py` 重切好矣，所以chit-má是純ê
負例）。**樣本猶少，拄著新ê病灶愛kā伊加入去重校**——數字囥佇
`blank_runs.py` ê `EDGE_INK`／`EDGE_SHARE`，改一擺就好。

圖條是囥佇 work dir（`kithann/out/mxf/<slug>.B.work/strips/`）ê，會
重生毋過嘛會予人清掉。Work dir 無矣ê集數，這支報「無圖條通量」，
**毋是**報「無代誌」——兩句話無仝款。

**看影片彼个（`scan_low`：解關鍵格，量 y 900–1035 有墨、y 722–844
無墨ê幅數）干焦會使做佐證，袂使做判準**——伊有兩款失手，兩款攏
量著矣：

- **假ê有**：規幅ê圖卡佇中央，帶內本底就無字。橫向限佇中央
  （x 500–1500）會使kā天氣框、紅色語別牌、受訪者名牌閬掉（032午
  本底 16 幅，限了賰 6 幅），毋過中央ê圖卡閬袂掉。
- **假ê無（較歹）**：版型顛倒彼款——**名牌peh起去帶內**，帶內就
  有墨矣，「帶內無墨」這條就無成立。057晚 就是按呢：空白連紲
  94 條、讀者抽原生格抄出實際ê字，掃描soah干焦報 20 秒。

所以順序是：**空白段掠出來 → 抽原生格證實**。掃描是提來揣「大約
佇佗位」ê，毋是提來判「有抑無」ê。

`verify_band.py` 掠袂著這款：伊是**逐集**一个判決、抽樣幾分鐘，
這款病規集大部份是好ê，干焦中央彼幾分鐘毋著。

#### 補讀無效，愛重切——`rescan_band.py`（2026-08-31 做ê）

055午 b02 佮 057晚 b02 兩个讀者各自講仝一句話：**彼段ê cue 邊界是
掠毋著區域ê ink 變化切ê**（衫仔ê白花紋、畫面晃動），佮字幕換行
無關係。所以**干焦提低位ê圖條來補讀是無效ê**——字補入去會湠佇
毋著ê時間頂懸。彼段愛用改過ê帶**重切 cue**。

使用者裁定（2026-08-31）走「只重切著病彼段」這條路，毋是規集重做。

    python3 -m scripts.news.rescan_band <work-dir stem> <lo> <hi>

伊做ê代誌：

1. 用 `--region 420,900,1500,135` kā [lo, hi] 彼段ê時間重切。
2. Kā新ê cue 接轉去舊ê排內底，規集重編 1..N。
3. 照仝一份對應表徙 `b*.tsv`（換掉彼段ê逝提掉、後壁ê重編號）
   佮 `strips/`（先徙去 staging 才換轉來，無ê話 400→402 會kā
   猶佇咧ê 402 蓋去）。
4. `sheets.json`、`transcripts.json`、`verified.json`、`sheets/`
   改名做 `.before-rescan` 抑是清掉——contact sheet 頂懸**印ê是
   舊號碼**，重編號了後彼幾張就毋是正確ê矣，愛重做。

**區域是 `420,900,1500,135`，毋是 `0,900,1920,135`。** 紅色語別牌
佮節目ê下三分之一就佇彼幾逝ê x < 400，靜態圖會kā Jaccard ê分母
灌大，真正ê變化就分袂出來。058晚 量過：規闊切出 220 條，內底
26 條短過半秒（是語別牌家己ê動畫）；限中央切出 152 條，短ê賰 7 條。
就是 README 頂懸彼條「crop 愛闊，判準愛狹」閣一遍。

#### 區域縮到 x≥420 ê代價：長句會hőng切著（量過，賰一條）

避開語別牌ê時，順紲kā畫面倒爿彼幾百 px 嘛閬掉矣，所以**傷長ê字幕
逝，倒爿會走出區域外口**。六集 972 條重切ê cue 內底，實在著ê
**干焦 1 條**：`20210224_055_午間_Cou_鄒` cue 376，讀者掠全幅原生格
救轉來——「我爸爸他們是（民國）41年槍斃的（應是43年）」。

我頭起先用「strip 頭三欄有墨」做判準，掠著 13 條，**12 條是誤報**：
彼是 x<420 ê**天氣框**（「滿州 18-26」這款）佮圖卡ê墨，毋是字。
八條本底就留空（標題帶、圖卡），賰彼幾條抽全幅格看過攏是完整ê
（「這是一個真實發生的故事」「我媽媽不知道有這個人」「都帶動」——
上尾彼條畫面就是三个字爾爾）。

**判準愛按呢寫**：strip 頭幾欄有墨**而且**彼格ê文字讀起來斷斷ê，
才去掠全幅格。單看像素會kā下三分之一ê圖掠做字。

#### 區域ê**懸度**有一个無聲ê懸崖：122 列

Contact sheet ê裝箱預算是 `int(1.10e6 / sheet 闊)`。一逝開
`10 + 懸度 + 2`，欲裝四條就愛 `4 × (12 + h) ≤ 預算`。

**預算是對 work dir 內底上闊彼張 strip 來ê，毋是對你這遍切ê區域。**
`sheets._sheet_width` 提ê是逐塊內底 `max(tile.width)`，而重切ê strip
是佮這集其他規闊ê strip 睏做伙。用重切區家己ê 1500 算，預算是 677，
`4 × 146 = 584` **會通過**——拄拄好kā實在發生過彼擺放過。

**Sheet ê闊是逐集無仝ê，毋是定數。** `_cue_blocks` kā逐張 strip 裁
到墨ê外框，所以闊度是彼集上長彼逝字幕。74 集量過：**26 集 2044**
（預算 538、h ≤ 122）、**48 集 1320**（預算 833、h ≤ 196）。我重切
彼六集**攏是 2044 彼款**，所以 135 才會予in加開三分之一ê圖條。

122 是上䆀ê情形，就照伊做——2044 過會去ê，1320 一定過會去。

**標準帶ê 122 毋是人隨便揀ê圓數，伊就是頂懸。** 賰ê餘裕干焦 2 px。

頭六集重切ê時我用 135（snap 做 134），裝箱掉做**逐張三條**，彼幾段
開 328 張圖條，本底 243 張就夠——**加開 85 張、約 18 萬 token，
了了ê**。字幕佇 y≈940–1010，用 122 列對 910 起算就罩會著矣。

`REGION` 已經改做 `420,910,1500,122`。平行 session 有做
`tests/news/test_sheet_packing.py` 顧 presets 這爿，逐个 preset 攏驗。

**這條懸崖恬恬**：三條抑是四條，圖條看起來完全仝款，讀者嘛照常讀
（in 有講「逐張 3 條毋是 4 條」，我彼時干焦當做提示愛修，無想著
是成本）。愛家己算才知。

**這是規條管線內底上危險ê一步**：規集ê cue 號碼會振動，`cues.json`、
`b*.tsv`、`transcripts.json`、`sheets.json`、`strips/` 五項攏是用號碼
做鍵ê。有一項無綴著，文字就落佇毋著ê時間頂懸，**逝數對、欄數對、
`ingest` ê覆蓋率嘛是 100%**，無一个所在會報錯。所以純算術彼部份
（splice／mapping／remap_rows）家己拆出來，21 條測試逐條驗過。

### 四之五之三、兩个位ê比例逐集無仝，閣有集數干焦一位

實測：

| 集 | 偏上 | 偏下 |
|---|---|---|
| 20210208_039午 | 10 逝（y 721–787） | 43 逝（y 778–845） |
| 20210209_040午 | 60 段（y 729–778） | 48 段（y 787–836） |
| 20210221_052午 | 118 段（y 728–779） | 65 段（y 786–837） |
| 20210217_048晚 | **0 段** | 188 段（y 787–836） |
| 20210209_040晚 | **0 段** | 142 段（y 787–836） |
| 20210219_050晚 | **0 段** | 165 段（y 787–836） |
| 20210217_048午 | **0 段** | 151 段（y 787–836） |
| 20210216_047晚 | **8 段**（仝一則包） | 126 段 |

**偏下ê位置真穩定**（字底 ~836）；**偏上會走**——20210212_043午 量著ê是 y 721–752（字底 752），比別集懸 26 px，**比例完全無穩定**：
對 0% 到 64% 攏有。

#### ★ 「兩个位攏有 ink」**毋等於**「兩个位攏有字」

這條ê所有數字攏是遮罩ê像素數，**佮「敢有字」是兩層代誌**。
2026-08-31 截圖ê時掠著一个現成ê例：

`20210221_052_午間_Kanakanavu_卡那卡那富` t=1248.76 本底記做「兩个位
攏有（10,501／10,494）」。掠原生格看——**畫面頂懸干焦偏上一逝
「繼續當職業軍人」，偏下無字**。彼 10,257 是**白色ê椅仔**。照欄位
拆開就真明：

    偏下 787–836   合計 10,257   倒爿 3,617   中央 6,640   正爿 0
                                （語別牌上緣）（椅背）

白椅仔ê光度夠、色彩夠中性（`white_min 185`、`max_spread 45` 兩條
攏過），閣有烏烏ê椅骹做外框，連 `outline` 彼條嘛騙過。桌仔、壁、
白衫仝款。

**所以欲判「敢是雙逝字幕」，袂使干焦看像素數**，愛看字ê形抑是
位置——比論字ê ink 是規排連紲ê、有固定ê字懸、橫向有字距ê節奏。
截圖是上緊ê法度。

（真正ê雙逝實例佇 `kithann/ocr/雙行/`：`20210212_043_午間_Rukai_魯凱`
t=727–730，族語羅馬字佇頂、華語佇下，倚正爿。）

**偏上是「逐則新聞包」ê版型，毋是規集ê屬性。** 20210216_047晚 有 8
段偏上，**8 段攏佇 t=468–631 秒仝一則記者包內底**；棚內訪談佮片尾
規个偏下。按呢就解說會通為啥物比例對 0 到 118 段攏有——看彼集內底
有幾則包用兩逝版型爾爾。

**假說ê生佮死。** 頭 6 集看起來「午間有兩个位、晚間無」（午間 3/3
有、晚間 3/3 無），我就kā伊寫入提示，順紲加一句「你這集若佮這个
講法無仝，特別報一下」。20210217_048午 推翻一半（午間煞 0 偏上，
伊閣提規集 1104 條 cue ê合成圖來證），20210216_047晚 推翻賰彼半
（晚間嘛會有）。**假說寫入提示ê時，順紲kā推翻伊ê條件嘛寫入去，
伊就會家己去予驗。**

**規則就是逐條 cue 家己量，莫照時段推。**所以「規集固定用佗一位」是行袂通ê，愛逐條 cue
家己量。20210209_040午 量ê結果是兩群ê質心分甲清清楚楚（777.2 佮
786.7 中間一个質心都無），按呢分 100% 會準。

### ★ 頂懸彼个結論愛修正：字幕帶有**兩个列位**

我頭一版寫「122 列內底 53% 是純背景、帶囥懸 65 列」。**彼是毋著ê。**
20210208_039午 ê讀者kā逐逝分開算，看出這集ê字幕有**兩个列位**：

```
偏下 y 778–845   43 逝（多數）
偏上 y 721–787   10 逝
```

122 列ê帶**拄好囥會落兩逝字**。所以彼片「純背景」毋是𨑨迌ê，是留
予偏上彼逝ê。我提「crop=1920:150:0:770」建議ê時，讀者當場指出彼
會kā偏上彼 10 逝剁掉幾若成——**伊是著ê**。

閣去看我家己印ê 20210208_039晚 剖面，720–749 遐本底就有一个較細
ê峰（+380、+603），我當做雜訊放掉。**彼毋是雜訊。**

**所以：**

- 「帶囥懸矣」**袂當按呢講**——伊是罩兩个列位。
- 「背景灌大分母」彼个機制**猶原成立**（逐條 cue 干焦用一逝，另外
  彼逝就是背景），毋過**袂使用縮帶來解決**，縮著就剁著字。
- 真正ê解法應該是**逐條 cue 揀伊家己彼逝**（先量該 cue ê ink 剖面，
  才決定用頂彼逝抑是下彼逝），毋是規集固定一个帶。這條猶未做。

48 集ê量測（下底彼張表）量ê是**混做伙ê結果**，所以「起點中位數
787」其實是「偏下較濟」ê反映，毋是帶囥毋著ê證據。

（4 集ê量測撞著取樣窗ê邊界，數字袂準；其他 44 集攏佇窗內。）

**猶未改。** 動著 `REGION` 就是動著全批ê 時間軸佮圖條，而且**已經交付
ê 60 集就是用這个帶做ê**。這是使用者ê決定。

### 四之五、「文化小辭典」段ê字幕沉落去，標準 crop 切無著

彼款段規个節目縮做**子母畫面**，字幕對 y≈722–844 沉到 **y≈825–885**。
標準ê `1920:122:0:722` 干焦切著頂懸約 19 px——遮罩 ink 對別位ê
5,000–20,000 掉到 **600–2,200**，看起來就親像「無字」。

**這解說ê是既有資料為啥物遮ê cue 空空抑是毋著：原本ê管線嘛看無。**
20210208_039晚 cue 273／285／286／302／319 攏是；cue 319 彼句
`年的轉折點` 三爿攏查無，就是按呢漏ê。

讀者ê做法是改用較懸ê crop。**毋過 y 700–880 猶原無夠**：
20210219_050晚 有 11 段子母畫面，字幕掉到 y≈803–857 閣較低，用
180 懸ê帶會kā下半截剁去（讀者是佇 contact sheet 看著字予削平才
發現ê）。這馬提示寫ê是 **`crop=1920:204:0:700`（y 700–904）**。

彼段ê判準嘛會予騙：字大半佇 840 以下，若照 786–840 算 ink 就會
判做「無字」，規段漏去。

根本ê做法無蓋歹：`reread.py`
ê `REGION` 是**預設值**，`cues.json` 內底ê `region` 鍵就會使逐集覆寫
（`reread.py:352`）。閣有，**一條 700–900 ê懸帶就兩爿攏罩會著**——
正常字幕 722–844、沉落去ê 825–885 攏佇內底。

代價是遮罩ê面積變大、背景雜訊嘛較濟，`min_ink` 這款門檻愛重掠。
**猶未做**——動著伊，全批圖條就愛重生。

### 四之五之五、逐段量剖面、只取彼一位——建議ê做法

使用者 2026-08-30 提ê：「先以多數為主，字幕辨識結果若真奇怪才做雙
層」。資料支持這个方向，而且會使閣較省：

| 集 | 偏上 | 偏下 | 多數彼位佔 |
|---|---|---|---|
| 048晚／040晚／050晚／048午／051晨／038晨／044晚 | 0 | 137–188 | **100%** |
| 043午 | 4 | 63 | 94% |
| 047晚 | 8 | 126 | 94% |
| 039午 | 10 | 43 | 81% |
| 040午 | 60 | 48 | 56% |
| 052午 | 118 | 65 | 64% |

**12 集內底 7 集完全免雙層**，閣 2 集干焦 4–8 段。真正愛ê是 040午、
052午 這款（偏上佔 36–64%）。

**毋過「等結果奇怪才重做」猶原較貴。** 「奇怪」本底就會使家己掠：
一段ê ink 若集中佇偏上、偏下差不多是零（040午 604.01 是懸 9,362／
下 0），逐段算一擺剖面就知。判斷成本佮「先做多數」仝款（攏愛算剖
面），毋過**省掉第二輪重做**。

所以建議：**逐段先算 ink 剖面 → 干焦提彼一位做圖條**。040午 量著
兩群ê質心完全無交疊（777.2 佮 786.7 中間一个都無），按呢分會準。

**仝一集內底位置無一致ê實例**（`kithann/ocr/偏上/兩个位實例.png` 有截圖）：

```
20210209_040_午間_Kavalan_噶瑪蘭.mkv
  t=2100.04  偏上（懸 9,362／下 0）      在店裡的擺設上面也設計的很多陷阱
  t=2599.40  偏下（懸 9,286／下 10,806） 因為景 櫻花這種東西

20210221_052_午間_Kanakanavu_卡那卡那富.mkv
  t=1302.72  偏上（懸 8,925／下 1,302）  現在年輕的一代很會說華語
  t= 936.40  偏上（懸 9,829／下 1,770）  多數原住民族語言的消失
  t=1248.76  **干焦偏上**（偏下彼 10,257 是白椅仔，毋是字——見頂懸
             ★ 彼條）                    繼續當職業軍人
```

### 四之五之四、Crop 愛闊，判準愛狹

抽格ê crop 提 `1920:204:0:700`（y 700–904）是為著**看會著**偏上佮子母
畫面段；毋過**算 ink／遮罩ê時，列愛限佇 y ≤ 845**。

因為下三分之一彼條紅帶ê懸低**逐集會走**：20210210_041晚 ê紅帶對
y≈848 就起，字佇 y 865–900、名牌佇 905–1000——攏落佇 crop 內底。
判準若無限，紅帶ê字就會予算做字幕。

「930–1004 是超文字」這个數字是佇 `20210212_043_午間_Rukai_魯凱` 佮
`20210209_040_午間_Kavalan_噶瑪蘭` 量ê，**莫死記**——逐集會走。

### 四之三之四、播出端ê錯字倒轉來是「無憑印象生」ê旁證

059晚 b02 是hőng額度斬斷、無行完自我核對彼份，後來補派一支去對。
彼支發現：檔案內底「總**统**」28 改、「總**統**」0 改，掠原生格
放大看，畫面燒印ê就是繁體「總」配簡體「统」。

伊講ê真著：**靠推測寫ê模型絕對會寫「總統」**。所以整齊出現ê
簡體字，倒轉來證明彼份是照畫面讀ê。

這比逝數欄數彼款結構檢查有路用濟——結構檢查擋袂牢憑印象生ê
（289–480 佮 481–576 鬥做伙彼份，逝數欄數攏對），這條會使。

**抽讀核對愛蓋著批ê尾溜。** 我指定 4 張ê時干焦罩到 cue 520，
尾段 521–576 無人顧；驗證彼支家己發現，加讀 4 張。圖對 context
提掉是**囤起來ê**效應，愈後壁愈korh會著，抽樣愛平勻，尾溜特別愛顧。

### 四之三之三、導播指令漏入去字幕流

059晨 cue 207 是 `秀出背板`——彼是講予控制室聽ê話，毋是播出去
予人聽ê。**毋過伊是燒印佇字幕帶內底，字型佮一般字幕仝款**，
所以「看伊佇佗位、看伊ê款」這兩條攏分袂出來。

讀者照「照畫面抄」寫入去，閣共伊標出來，這款做法著：分袂出來
ê時，寫入去閣講一聲，勝過恬恬提掉。留空是**看袂出來ê**——
後手無法度知影彼格是「無字」抑是「有字毋過我判做毋是對白」。

規矩：**這款漏入來ê物件照抄，佇報告內底標出來**，予人後壁決定。

### 四之三之二、分「播出端ê錯字」佮「這集ê體例」——看伊一致無

讀者ê規矩是「照畫面抄，莫訂正」，毋過欲照抄進前先愛掠會準：
遮真正是彼字，抑是我看毋著？兩个法度，攏是 054晚 b07 ê讀者做ê：

1. **佮仝一逝、仝一批ê字並排比。** `統` 對 `统`——`糸` 佮 `纟` 排做
   伙就看會出。`経` 對 `經`——提仝一批 cue 761 ê `輕`，右半ê筆形
   一致，就知影彼字是正體，毋是日式（055午 b03 用這法排除一个
   誤判）。單看一格ê時，圖條ê解析度無夠決定；有對照就夠。
2. **一集內底一致ê寫法是體例，毋是錯字。** 054晚 ê `占`（無 `亻`）
   佇 621／642／861 三改攏仝款，所以彼是這集ê寫法。反過來講，
   仝一集內底孤一改無仝ê（821 ê `统`，其他所在攏 `統`），才是
   播出端ê錯字。

第二條ê意思是：**錯字愛看規集才判會準**，孤一批ê讀者看袂著全貌。
所以報告內底ê「掠著ê錯字」是候選，毋是定論；集數讀了轉來看
逐批ê報告，才有法度分。

### 四之三、字幕帶內底毋是對白ê物件，留空

字幕帶（y 722–844）內底毋是逐項攏是對白。下底這幾款愛留空，**莫寫
入去**——寫入去就變 junk cue，佇語音辨識ê訓練資料內底是雜訊：

- **片尾跑馬職稱**（導播／剪接／梳化造型…）。20210206_037午 cue
  1167／1170、20210201_032午 cue 1082 攏是。既有資料這幾條嘛是空ê。
- **「文化小辭典」ê超文字**：伊佇 y≈940–1010，**佇 crop ê下底**，
  所以字幕帶彼位本底就無字（20210201_032午 cue 318，17 格）。
- **畫面提供／受訪者名牌**這款貼佇帶頂懸抑是紅色下三分之一ê字。

判準：**看伊是毋是有人講出來ê話。** 講ê才是對白。若無把握，看既有
資料彼幾條是毋是嘛空ê——空ê就是共識。

### 五、既有資料本身ê錯，愛講出來

「莫修正錯字」指ê是**播出端本底就有ê錯字**（「不知到」「迫不急待」
「彌猴」），彼款照抄。

毋過**既有辨識掠毋著ê**愛回報，莫恬恬仔沿用。20210215_046 三位讀者
各自確認既有kā「牠」認做「牝」，其中一位放大 5 倍佮 Noto Serif CJK
對照字形；閣較有力ê是既有家己就無一致（仝一集 cue 290 寫「牠」、
136/139/146/100 寫「牝」，畫面頂懸是仝一个字形）。

**毋過門檻愛掠予絚**：會使加入 `resplit.FIXES` ê條件是既有資料
**家己相矛盾**（仝一字形佇仝一集寫兩種）。20210213_044 就有反面例——
`者老`／`耆老` 佇全語料庫攏真實存在（耆老 145 擺、者老 18 擺），畫面
放大 4 倍確認就是「者老」，彼是播出端家己ê寫法。若kā伊正規化，是kā
著ê改做毋著ê。

比對ê時 `resplit.normalise()` 會先kā這款正規化，若無，把關會kā**刻意
ê更正**當做落字擋落來。

**閣有一款是頂懸彼句ê鏡像：既有kā播出端ê寫法「訂正」掉去。**
20210206_037午 揣著六條，攏是單段 cue（`safe_resplit` 無動，靠
`resplit.unsplit_but_changed` 報出來ê）：

| cue | 既有 | 畫面（放大 6 倍） |
|---|---|---|
| 695、706、707、708、711 | 半形 `(收穫如何)` | **全形 `（收穫如何）`** |
| 1021 | 耆老 | **者老**（耂＋日，無「耆」彼撇匕） |

**毋過愛逐條看畫面，莫掠做半形一定是毋著ê。** 20210201_032晚 這集
**真正有濫用**：cue 486 `(上下網線用什麼材料)`、294.01 `(靜浦部落)`
是半形，cue 499 `（為什麼買那麼多網）` 是全形，**既有三條攏對**。
所以判準猶原是「照畫面」，毋是「一律換做全形」。

仝款ê閣有：20210201_032午 cue 462（`者老`／`耆老`）、20210201_032晚
cue 291（畫面 `Cacepo’an` 用ê是彎ê U+2019，既有寫直ê U+0027；讀者
量像素：o 收尾到 a 起頭 69px ≒ 全形格 60.5＋字距 8）。**攏是既有kā
畫面ê寫法換做「較正常」ê寫法。**

兩爿ê證據攏齊：既有家己相矛盾（仝一句佇 cue 712／720／721 寫做全形），
畫面閣直接證實。**判準無變，是方向倒péng**：頂懸彼條顧ê是「莫kā播出
端ê錯字改做著ê」，這條掠ê是「既有已經kā伊改做著ê矣，愛改倒轉來」。
兩條攏是仝一句話——**照畫面**。

這款愛動著已經交付ê SRT 內容，毋是「切開切毋著ê cue」，所以
`safe_resplit` 干焦報，無寫。

### 五之二、報告著、檔案毋著

讀者ê報告佮伊寫入ê檔案**會無仝**。20210213_044 cue 105 就是按呢：
報告內底清楚寫「三句：(你也是來買菜嗎) → 是 來買菜的 →
(為什麼要來買菜)，切點 180.55」，毋過寫入檔案ê時 105.01 佮 105.02
兩格填仝一句，既有ê「(你也是來買菜嗎)」煞無著落。

**莫干焦讀報告就相信**。`safe_resplit` ê把關是掠這款ê唯一一關——
伊比對ê是**檔案**，毋是報告。重讀頭前彼幾集我攏干焦看報告，這擺是
把關擋落來才發現。（實例：`20210206_037_晨間_Thau_邵` cue 498、`20210219_050_午間_Rukai_魯凱` cue 1115／1116。）

### 六、判「查無」進前愛看清楚逐字稿ê範圍

彼陣逐字稿有兩爿（視覺辨識佮新聞稿供字ê疊層），干焦對頭一爿就會kā
文稿供字彼寡誤報做漏字——20210207_038 頭一擺算出 36 句，過濾了賰
3 句。疊層已經提掉矣（量過是全然重複ê），這馬 `2-vision/` 就是全部，
毋過教示留咧：**判「查無」進前，先確定家己對ê是規个逐字稿。**

## 文字全部來自 Claude 視覺辨識

成品裡沒有 tesseract 的字，也沒有文稿的字。過程中兩者都試過，結論寫在下面。

### tesseract：只堪定位，不堪供字

讀這種壓在畫面上的中文，46 個手讀 cue 的基準是**逐行 26% 正確**
（`去年底桃園復興巴陵的一場大火` → `同/和)[人圖復興叫陜病一易大六`）。
最初拿它當「夠爛但不亂」的訊號去對齊文稿，後來連這個角色也不需要了。

### 文稿：當交叉驗證有用，當文字來源沒用

> **程式已移除。** `align.py`／`rtf.py`／`rtf_sheets.py`／`compare_rtf.py`／
> `check_align.py`（894 行）與其測試（375 行）都刪掉了。要看它們，
> checkout **`da2f0b3`**——那是最後一個含這批程式的 commit。比對結果本身留著：
> 比對結果本身也已經從 store 刪除（結論寫在
> `Kari-SRT/news/1-ocr/README.md`）；要看檔案本體翻 git 歷史。

`.rtf` 新聞稿的內容確實就是字幕的來源，`align.py` 也真的能把 cue 對回稿子
（3-gram 投票找位置，再解一次最長遞增子序列強迫單調）。但**逐字忠實於畫面
這件事，稿子在原理上做不到**。

4,344 個原本由文稿供字的 cue，全部重新用視覺讀一次逐字比對：

| | |
|---|---|
| 一致 | 4,008（92.3%）|
| 有差異 | 336（7.7%）|

336 筆全部是畫面對、稿子錯，而且分成兩種性質：

- **58% 是切點問題**：旁白在稿子裡是一整段，稿子沒有記錄字幕師把它斷在
  哪，所以只能照字元位置切，切出來多字或少字。
- **40% 是字幕師改過稿**：稿子 `然後千萬不要裝紙`，畫面是 **`裝死`**（稿子
  打錯字，字幕師改對了）；`看到熊3件事不要做` → **`3件事情不要做`**；
  `有時可能會胃痙攣` → **`有可能會胃痙攣`**。這類不管對齊做得多好都救不回來。

還有 1 筆是稿子把片尾 `企劃/文字 黃慧如` 當字幕給了出去，畫面其實是空的。

**所以文稿對「產生字幕」沒有淨幫助**——先用它供字、再全部重讀，等於把同一批
sheet 讀了一遍，還多花了建兩次 contact sheet 的工。真正的價值是**當獨立
對照**：4,344 行、92.3% 一致，而且不一致的部分有系統性解釋，這是目前唯一
能證明視覺辨識可信的證據。

比對報告：`Kari-SRT/report/rtf-vs-vision.md`（分析與範例）、
`rtf-vs-vision.json`（完整 336 筆）。

### 視覺辨識的三個實作重點

- **cue 編號會跳。** 分批重讀時 sheet 上只放需要的 cue，編號不連續
  （214、219、533…）。prompt 必須明講「讀 gutter 上的數字，不要自己 +1
  補號」，否則整段字幕會靜靜落在錯的位置——比讀錯字嚴重，因為不會報錯。
  `ingest.py` 再擋一層：TSV 裡出現不屬於該批 sheet 的 cue，整批拒收。
- **一批 24 張、TSV 直接寫進磁碟。** 實測每張 2,788 tokens（12 張一批時是
  3,450，prompt 分攤較差）。主對話只收行數，不然 context 會被圖吃光。
- **空白 cue 的行尾 tab 會被 Write 工具剪掉**，`ingest.py` 匯入前統一補回。
  prompt 要叫 subagent「寫一次就好、不要自己修 tab」，否則有 agent 會為了
  補 tab 反覆改檔，用掉 16 萬 tokens（正常 6.4 萬）。

## 檔案

| | |
|---|---|
| `add_episodes.py` | 逐支指定路徑寫 inventory；**合併不覆蓋**，已在的跳過 |
| `add_episodes.py` | 補登 SFTP 抓來的集數（影片已刪，只剩檔名可查）|
| `run_cues.sh` | 複製到本機 → 切 cue |
| `gap_sheets.py` | 把全部 cue 做成 contact sheet（`.B.work`）|
| `batches.py` | 列出一集還沒讀的 sheet，切成 24 張一批 |
| `ingest.py` | 驗證 TSV（格式＋cue 編號歸屬）並匯入 |
| `make_srt.py` | 單集組裝 SRT |
| `make_all.py` | 全部組裝＋寫進度表工作版 |
| `tracker.py` | `smkul.csv` 的欄位與單列組法，三方共用 |
| `sftp.sh` | SFTP 包裝：密碼只以檔案存在，處理 BatchMode／askpass 兩個坑 |
| `sftp-askpass.sh` | 給 OpenSSH 讀密碼檔的 hook（`SSH_ASKPASS`）|
| `fetch_sftp.sh` | 吃播出月份；逐集：下載 → 驗位元組 → 驗band → 切cue → 精修 → **刪影片** |
| `refine_cues.py` | 邊界精修：0.2s 粗切 → 25fps 逐幀分類 → ≤0.05s |
| `verify_band.py` | 量列剖面，確認字幕帶真的在 preset 說的位置 |
| `blank_runs.py` | 掠 vision TSV 內底ê長連紲空白——字幕若印佇帶外，規段會變空白（離線，免影片）|
| `rescan_band.py` | kā一段用毋著帶切ê cue 重切、接轉去、規集重新編號（五項用號碼做鍵ê物件做伙徙）|
| `split_cue.py` | 一條 cue 內底有兩句ê時，佇量出來ê彼點kā伊剖開（`images` 留原本ê，strip 免改名）|
| `migrate_strips.py` | Strip 檔名對序號換做起始時間；`scripts/ocr/stripname.py` 是號名ê所在 |
| `migrate_workdirs.py` | 舊 work dir ê平 `cues.json` 徙入階段目錄（帶 `refined` ê入 `2-refined/`，無ê入 `1-cues/`）；冪等，做過矣（news 75 个、《開會了》40 个）|
| `redump_store.py` | 店面ê JSON 重排做人讀有ê形（縮排、鍵排序、漢字免跳脫）；JSONL 一逝一筆免縮排。干焦改排版，內容無動 |
| `plan_month.py` | 一批＝一个播出月份：揀來源、登記 pending、出跳過報告 |
| `sources.py` | 一集配一支檔的四條規則；揀袂出來就跳過並回報 |
| `resolve_slug.py` | 用 `ilrdf-corpus.csv` 把路徑對成 work dir／SRT 名稱 |
| `paths.py` | 全部路徑的單一出處；`stage_path()` 是階段目錄唯一出口；`--var` 供 shell 取值 |
| `publish.py` | 整批把關→清 `pending`、遷 `cues`、定版 `smkul.csv` |
| `rebuild.py` | 從 Kari-SRT 離線重建全部 SRT 並逐 byte 驗證 |
| `Kari-SRT/news/1-ocr/2-vision/` | 視覺逐字稿 TSV（全部 cue，文字唯一來源）|

`2-vision/` 就是全部 cue 的完整逐字稿，是這批工作最可重複使用的成果，
正本在 `Kari-SRT/`。

## 產出

正本都在 `Kari-SRT/`（submodule，進版本控制）：

逐集檔案都在階段目錄下的**播出月份**一層（`<年-月>/`），跨集的與總表
不分層：

- `news/1-ocr/3-srt/<年-月>/<播出日期>_<集數>_<時段>_<族語英>_<族語中>.srt`
- `news/smkul.csv` —— 進度表（含影片長度欄，由時間軸推導）
- `news/1-ocr/{1-cues,2-vision}/<年-月>/`、
  `news/inventory.json` —— 重建 SRT 所需的全部過程資料

驗證離線閉環：`python3 -m scripts.news.rebuild --verify` 會只用
Kari-SRT 的資料重組全部 SRT 並逐 byte 比對，缺件即指名失敗。

### 時間精度與留白

- **邊界精度**：`cues` 粗切在 0.2s 格點（5fps）；`refine_cues.py` 在每個
  邊界 ±0.24s 窗內以 25fps 逐幀分類（不重切、不動 cue 集合與文字），
  精修到 ≤0.05s，manifest 記 `refined` 與 `duration`。新月份由
  `fetch_sftp.sh` 在刪影片前自動跑。
- **SRT 留白**：組裝時每句前後各延伸至多 0.5s（CLAUDE.md 規定，對齊
  Kaldi `--max-edge-silence-length` 預設）；間隔不足在中點相接，
  截短於 `[0, duration]`。留白只在 SRT 輸出，`cues.json` 是真實切換點。

## 換機器要帶什麼

進 git 的（跟著 repo 走，不用管）：主 repo（程式 `scripts/`、測試
`tests/`、`.claude/` 說明）＋ `Kari-SRT` submodule（上面那些資料正本，
**含 `inventory.json`**——它已經不在主 repo 了，不必另外搬）。

**不在 git、要自己搬或重建的**：

| | 怎麼辦 |
|---|---|
| `kithann/out/mxf/*.work/` | 純快取（strips/sheets 約 15 GB）。**不用搬**，`rebuild --verify` 保證正本可離線重生 |
| `Kari-SRT/ilrdf-corpus.csv` | 節目目錄正本，`resolve_slug.py`、`plan_month.py` 都要它 |
| `.sftp-pass` | 故意不進 git。到新機器**自己在終端機重建**，不要叫 Claude 寫 |

新機器上還要做的：

1. `sudo apt install ffmpeg tesseract-ocr tesseract-ocr-chi-tra python3-venv`
   ＋ `python3 -m venv ~/.venvs/subs2srt && ~/.venvs/subs2srt/bin/pip install numpy Pillow`
   （devcontainer 重建過一次就掉光，這是實際發生過的）
2. 重新 `ssh-keyscan 192.168.35.10 >> ~/.ssh/known_hosts`
3. `.devcontainer/devcontainer.json` 裡那條 `ilrdf-corpus` 的 bind mount 是舊機器的
   路徑，新機器如果只用 SFTP 就可以整條拿掉

## 切 cue（`ocr-cli cues`）ê兩个坑

2026-08-30 對頭做 054–059 這 14 集ê時踏著ê。

### 一、`cues` 內底ê ffmpeg 無 `-nostdin`——佇迴圈內底會恬恬切一半就停

`while read … done < list` 這款迴圈內底叫 `ocr-cli cues`，伊內部ê
ffmpeg 會kā迴圈ê stdin 食去，切 cue 就**停佇半路**：

```
20210223_054晚（影片 2910 秒）
  無關 stdin： 232 條 cue，上尾一條煞佇 720.4 秒
  < /dev/null：1,097 條 cue，罩牢規个 2910 秒
```

**上歹ê是伊袂報錯**：`cues.json` 寫出來是好ê、`sheets/` 嘛有，看
起來完全正常，干焦內容少 4 倍。愛按呢寫：

```bash
PYTHONPATH=. .tox/rebuild/bin/python -m scripts.ocr.cli cues \
  "$MKV" -o "$WORK" --presets scripts/news/presets.json \
  --preset titv-news --sheets < /dev/null
```

**驗收**：切了後對 `cues.json` 算「上尾一條ê end」佮影片長度比，差
傷濟就是予人食著。

（這條佮提示正本內底叫讀者加 `-nostdin` 是仝一个坑；我家己ê腳本無
防，就中矣。）

### 二、`--preset` 愛家己指定，莫靠檔名比對

`presets.json` ê `titv-news` 是比對**檔名內底ê `NL00`**（原始來源
`21NL003_54午間族語新聞.mxf`）。`kithann/out/mkv/` ê檔名是用
`srt_name` 號ê（`20210223_054_午間_Kavalan_噶瑪蘭.mkv`），比袂著；
無指定ê時伊會家己偵測，出來是 `0,588,1920,436`——436 列懸，紅帶佮
超文字攏含入去。

## 派視覺辨識ê兩个省法（2026-08-30 量ê）

### 一、一批 72 張，毋是 24 張——省 53%

仝一集（054，午間佮晚間）ê對照，仝一款提示、仝一款把關：

| 一批幾張 | 支數 | token 中位 | **逐張 token** |
|---|---|---|---|
| 24 | 6 | 88,772 | **3,551–4,015** |
| 72 | 3 | 130,127 | **1,873** |

工具用ê密度是**仝款ê**（24 張組 2.9 次/張、72 張組 3.1 次/張），
所以省ê毋是覆核，是**逐支 agent ê固定成本**——讀提示、驗抽格管線、
建遮罩、量列位、寫報告，遮ê佮伊讀 24 張抑是 72 張無關。
這佮「做大集比做小集划算 2.6 倍」（`kithann/out/token-report.md` §二）
是仝一件代誌，干焦是這遍有仝集ê對照，變異洗掉矣。

**正確率無看著差別**：兩組ê TSV 攏過仝一道欄數／逝數把關，
`ingest` ê gap 覆蓋率攏是 100%。

72 是頂懸——閣較大就會去挵著單支ê時間佮中斷風險（一支 72 張ê
走 18–20 分鐘）。

**尾批莫留細个。** 三个點排起來真清楚：

| 一批幾張 | token | **逐張** |
|---|---|---|
| 6（055晚 b05） | 47,623 | **7,937** |
| 16（055午 b04） | 97,953 | **6,122** |
| 72（三支ê中位） | 130,127 | **1,873** |

仝一个固定成本，工課賰十二分之一。
所以尾批若無到 ~24 張，就kā伊倂入前一批（72/72/72/**78**，
毋是 72/72/72/72/**6**）。倂入了後彼批較大，逐張ê價數猶原較俗。

### 三、讀圖條一擺 4 張——濟過就會恬恬落勾

055晚 b03 ê讀者一擺讀 6 張，挵著 `media removed: request limit`：
sheet_145–192 ê圖對 context 內底提掉，賰尺寸註記。伊發現了改做
4 張一批、逐批寫暫存檔，才無落勾。

**歹ê所在是伊恬恬**：模型無看著圖矣，毋過提示、cue 號碼、頭前
讀ê內容攏猶佇咧，所以伊猶原寫會出來——寫ê是「照頭前ê款推ê」，
毋是畫面頂ê字。TSV ê逝數佮欄數攏會對，`ingest` mā 袂出聲。

已經寫入 `vision_tools/brief.md`。

### 三之二、共用 scratchpad ＋ 圖掉去＝**看起來完全正常ê假資料**

2026-08-30 險過一擺，值得記牢。兩件代誌鬥做伙：

1. Scratchpad 是逐支 agent 公家用ê，我發ê提示叫 in 寫 checkpoint
   soah 無規定檔名愛帶集數，所以 058午 b02 佮 057晚 b02 兩支攏寫
   `scratchpad/b02/`，互相蓋。
2. `media removed` 一著，圖對 context 內底提掉，模型猶原寫會出來。

結果 scratchpad 內底生出一份 `final_b02.tsv`：**289–480 是 058午ê、
481–576 是 057晚ê**。逝數對、欄數對、cue 號碼連紲，`ingest` 袂出聲，
**看起來完全正常**——干焦內容是兩集鬥起來ê。

這遍無寫入去，因為兩个讀者攏家己發現：058午 b02 彼支看著彼份檔
內容佮家己無仝，無提來用，閣重讀三張核對；057晚 b02 彼支發現
checkpoint hőng蓋去，改用家己ê context 重起。**兩爿攏靠讀者細膩，
無靠管線。**

已經修ê：`vision_tools/prompt.py` 出ê提示，checkpoint 路徑固定是
`{scratchpad}/{集數}-{批號}/`（`{scratch}` 這个鍵），逐支家己一份。

### 二、提示囥檔案，叫 agent 家己去讀

提示一份 3k token。本底是kā規份貼入去 `Agent` ê `prompt`，主迴圈
逐批就開 3k；改做

```
頭一件代誌：Read `<提示檔路徑>`。……讀了就照伊做。
```

主迴圈逐批賰 ~40 token。一个月 45 批省 135k，1、2 兩月 ~420 批
省 **1.2M**。提示本身 agent 猶原愛讀，所以省ê是**主迴圈**彼爿——
毋過主迴圈本底才佔 4–5%，這條ê意義是「主迴圈ê 4–5% 內底，
提示彼半消去」。

提示是 `scripts/news/vision_tools/prompt.py` 產ê，逐批一个檔，
囥佇 scratchpad。

### 五之三、族語人名ê分隔號——**使用者裁定用 `．`（U+FF0E）**

2026-08-31 裁定。已交付彼 60 集嘛照按呢改過矣（`·` 54 改、`‧` 22 改、
`・` 1 改，全部換做 `．`），SRT 重建過、`rebuild --verify` 過。

因端：這粒點有四五个碼位生做差不多（`·` U+00B7、`‧` U+2027、
`・` U+30FB、`．` U+FF0E），**看是分袂出來、量嘛量袂清**——捌有兩个
讀者，一个量字寬揀 `‧`、一个照 repo 頻率揀 `·`，兩爿ê理路攏講會通。
對下游來講 in 是無仝物件（ASR 訓練文本、揣人名、`name_catalogue`
比對），所以愛釘做一款，袂使交予讀者家己判。

`vision_tools/brief.md` 已經釘死。

## Strip ê檔名是**起始時間**，毋是 cue 編號

`strips/t01874700_han.png` ＝ 這條 cue 對 1874.700 秒起。

**因端**：cue 編號會振動。`safe_resplit`（重讀）、`rescan_band`、
`split_cue`，任何一支kā cue 拆開ê，後壁ê編號攏會退位，毋過磁碟頂
彼支檔ê名是切ê時陣號ê。2026-08-31 量過：**75,290 條內底有 46,665
條（62%、48 集）ê檔名佮 cue 編號無仝**。`strips/00844_han.png`
對著ê是 cue 951。

兩爿ê號碼攏四位數、範圍相疊，**看毋著ê彼个看起來完全正常**。
這份 README 頂懸記ê 6 條「掠會轉來ê句」，cue 號碼寫ê攏是 strip
編號——051晚 記「844」實際是 951，差一百外條；照按呢去改會改著
完全無關ê cue，**無一个所在會報錯**。

**時間袂綴重新編號走**（別條 cue 拆開ê時，這條ê起止時間原封不動），
而且 `t01874700` 看起來就無成 cue 號碼。使用者裁定 2026-08-31：
新ê舊ê攏換。

- 號名ê所在：`scripts/ocr/stripname.py`
- 遷移：`scripts/news/migrate_strips.py`（改名 70,401 張，做過矣；
  136 个 work dir 全部時間命名，0 个舊序號、0 个濫做伙）

**Contact sheet 嘛仝款矣**（2026-09-03）。`sheet_003.png` 是「這批
內底第幾張」，毋是識別碼——cue 一剖開，sheet 重生，第三張就是別段
節目矣。改做 `t<毫秒八碼>.png`，用該張頭一格 cue ê起始時間。

    python3 -m scripts.news.migrate_workdirs --sheets --write

改名佮 `sheets.json` ê鍵是**做伙改ê**：彼份對照ê鍵就是檔名，干焦改
一爿對照就斷去。實跑：news 7,191 張、《開會了》9,029 張，攏改煞，
舊命名賰 0 張，檔案總數無變。讀 sheet ê程式（`batches.py`、
`aiyalaeho/ingest.py`）本底就是讀 `sheets.json` ê對照，無看檔名，
所以無受影響。

遷移期間有過一條護欄，掃規批ê `images` 看指ê檔案有無真正佇磁碟頂。
遷移做煞就提掉矣：**伊斷言ê對象是 `kithann/` ê暫存內容**——別條線
重生圖條ê時伊就紅，毋是程式歹去。快取ê內容毋是測試通顧ê物件。

**猶原ê規矩**：欲對 strip 揣 cue，讀 `cues.json` ê `images` 欄。
換名了後檔名袂閣騙人，毋過 `images` 猶原是唯一ê正解——一條 cue
拆做幾若條ê時，逐半攏指仝一張原圖（032午 cue 108–111 攏指仝一張），
彼款關係干焦 `images` 講會清楚。

#### 遷移ê時掠著ê三項

1. **`.B.work/strips` 是 symlink 指去 `.work/strips`**——兩个 work
   dir 公家一批檔。改一爿ê時是穿過 symlink 改著實體檔，另外彼爿ê
   `cues.json` 就落後矣。判準：新名有、舊名無 ＝ 已經改好。
2. **`.C.work`（16 个）是舊ê、無仝ê切法**（1085 條對 1188 條），
   strips 是 symlink 指去公家彼跡，時間對袂起來。交付流程無碰伊，
   刣掉矣。
3. **孤兒 4,889 張**——拆過 cue 了後，原本彼張無人閣指。刣掉，
   才袂兩套命名同時佇咧。

## 還沒做的

**十七集內底掠會轉來ê 6 句，猶未寫入。** 量法佮清單見「四之二」。
彼六句攏佇**已經套用過ê**集數，所以袂使閣直接走 `safe_resplit`
（cues.json ê編號已經振動過，`segments.tsv` ê編號是舊ê）。等規批
讀了才做一擺，逐句家己覆核原生格：

| 集 | 句 | 型 |
|---|---|---|
| 20210205_036_午間 | 小年夜各地都容易下雨 | 切袂開（亮氣象圖） |
| 20210205_036_午間 | 民眾接種狀況等 | 切袂開 |
| 20210219_050_午間 | 中部9至26度 | 切袂開（cue 1115） |
| 20210219_050_午間 | 東部10至25度 | 切袂開（cue 1116） |
| 20210218_049_午間 | 自己掏金錢 | 規句厝邊攏無（cue 806） |
| 20210220_051_晚間 | 讓他使用它 | 規句厝邊攏無（cue 844，既有 843／844／845 仝一句排三擺） |

**「文化小辭典」段ê 字幕位置**（見「四之五」）：字幕沉到 y≈825–885，
標準帶切無著。`cues.json` ê `region` 鍵會使逐集覆寫，一條 700–900
ê懸帶兩爿攏罩會著；門檻愛重掠、圖條愛重生。讀者這馬是家己改 crop
應付ê。猶未做。

**`reread` ê切段器會使改用 XOR 抑是召回率當做分界判準**（見「四之
二之二」）。這會kā「切袂開、讀者只好kā兩句相黏」彼款減少——彼是
這條線目前上大ê漏空。猶未做。

**既有「濟記」ê彼款——2026-08-31 查過矣，佮頭起先想ê無仝。**

本底記ê四條（035晚 775、049午 22、038晚 1074–1087、037晨 961）內底，
頭前三條佇重讀了後已經無矣抑是徙位。閣去查ê時，**掠著ê毋是幽靈句，
是兩款別項**：

**一、新聞標題卡hőng當做對白。** 20210207_038_晚間_Pinuyumayan_卑南
cue 1170–1182 記著 `大武鄉推生態旅遊 山豬窟"千年神榕"成焦點` 十三改、
23.1 秒。抽原生格看：彼是**新聞標題條**——倚左、外框較幼、字懸差不多
是對白ê一半（46 px 對這集ê中位 67 px）。伊就佇字幕帶內底，字型嘛成，
所以「看伊佇佗位」佮「看伊ê款」兩條攏分袂出來。

**孤一集內底量字懸，分會開。** 038晚 彼張標題卡量著 46 px，這集
對白ê中位是 67；059晚 b03 ê讀者家己量著 ~30 對 59–60。所以你若
懷疑一逝是標題卡，量伊就會使判。

#### 毋過**規批自動篩袂通**——試過，模組已經刣掉

我做過一支 `line_height.py`，用「字懸無到這集中位ê 70%」做篩，
掃規批出 **9,483 條**，無路用。因端是**字懸是雙峰ê**：
20210228_059_晨間_Hla'alua_拉阿魯哇 量出來 122 px 有 232 條、52 px
有 189 條——單逝字幕佮雙逝字幕兩群。中位落佇 104，兩爿ê群攏hőng
掠做「無正常」。

用「較低彼群」做基準嘛袂穩：038晚 ê標題卡是 46、對白 67，門檻掠
0.7 拄拄好是 47，差一 px。

**莫閣做這款規批自動篩。** 量字懸是**驗一條懷疑ê**ê法度，毋是
篩ê法度。真正欲篩，愛掠ê是「倚左」——標題卡倚左、對白徛中央——
彼猶未試過。

**二、賰ê「重複真濟改」大部份是真ê。** 20210105_005_午間_Kavalan_噶瑪蘭
cue 491–497「免於靈界干擾」排七改、53.1 秒——看 strip，**字真正佇畫面
頂懸**（倚正爿）。彼毋是幽靈句，是**一條 cue 傷長**（cue 497 家己就
47.4 秒），佮頂懸「補文字不等於修好」講ê是仝一件代誌。
20210210_041_晚間_Sakizaya_撒奇萊雅 ê `(襯詞)` 排五改、42.6 秒嘛仝款——
唱歌彼段播出端就是按呢標ê。

#### 試過行袂通ê：用 `cues.json` ê `ink` 掠幽靈句

想講「有字soah無墨」就是幽靈句，量了**無效**：20210218_049_午間_Truku_太魯閣
空白 cue ê ink 中位是 8,213，比有字 cue ê P25（6,743）**閣較懸**——ink
是規條帶ê墨，圖卡、天氣框、名牌攏算入去。莫閣試這條。

**「既有kā播出端寫法訂正掉」這馬開始有代價矣。** 20210208_039晚
cue 298 予把關擋落來——讀者讀著「者老」、既有寫「耆老」，把關就
認為「聽部落**耆**老早期的說法」彼句會無去。連紲害著彼條 cue ê
兩句新句（`啊呀漢人今天都休息`、`準備吃團圓飯了`）入袂了正本。
`resplit.FIXES` 這馬干焦有 `牝→牠`；`者老` 是**故意無囥入去**ê
（兩種寫法佇語料庫攏真實存在），所以這條愛人判。

**燒印字幕有「族語羅馬字 ＋ 華語」兩逝並排ê，猶未決定按怎收。**
20210212_043午 有 4 逝按呢（攏佇仝一則現場演唱ê記者包）：

```
偏上 barengerane ku Sanai      ← 族語羅馬字
偏下 思念的歌                    ← 華語
```

這佮進前記ê「兩个列位」**毋是仝一件代誌**：進前彼款是「一逝字幕
會囥懸抑是囥低」（相排斥），這款是**兩逝做伙出現**。

既有管線干焦記偏下；交付ê SRT **39,256 塊，一塊超過一逝ê都無**。
所以我這馬照慣例寫華語就好，族語彼逝先囥咧。**毋過彼是真正ê族語
文本**——這个案ê目標本來就是族語語料，畫面頂懸有族語ê所在真罕。
欲按怎收（另外開一个欄？另外一个 stage？）愛你決定。

（相關：全批 51,165 格內底有 231 格是純羅馬字，見「burned-in 字幕」
彼節。彼是「規格干焦羅馬字」，這是「兩逝並排」，兩款無仝。）

**語料庫本底就無teh排斥族語。** 20210214_045晚 規集ê字幕差不多攏是
羅馬字卑南語聖歌（`amawu iyu na kinakuwakuwa`、`hosana`），**既有
照收**。所以欲判ê毋是「欲收族語無」，是**「一格會使囥兩逝無」**——
族語彼逝若是彼格唯一ê內容，既有就收矣；干焦「兩逝並排」ê時，頂
彼逝予人放捒。

**Cue ê起點傷慢——字無掉，時間軸毋著。** 20210216_047午 兩个空縫
內底有字，毋過彼毋是漏去ê句，是**該條 cue ê起點晚 2.4–3.2 秒**：

```
cue 24   「喜歡大自然及原住民」畫面 ~121.6 出來，cue 24 對 124.80 才起
cue 433  「噶瑪蘭祖先因應生活需要」~1318.2 出來，cue 433 對 1320.60 才起
```

字攏佇正本，所以**把關看袂著**（`survives` 干焦掠落字）；`rebuild
--verify` 嘛袂講話（伊比ê是重建佮交付相仝無，毋是佮畫面相仝無）。
毋過對語音辨識ê訓練資料來講，字幕慢 3 秒就是聲佮字對袂起來。

這佮「邊界咬入去 0.2–1.0 秒」彼條是仝一類（`refine_cues` ê範圍），
數量猶未量過。

**Cue ê空縫內底有真ê句，無位通囥。** 20210213_044晨「2個選擇」佇
231.5–232.07，毋過 cue 75 收佇 227.40、cue 76 起佇 232.00——彼句
大半落佇**兩條 cue 中間ê空縫**，`reread` ê段罩袂著，既有嘛查無。
這是進前記ê「cue 空縫」彼類ê頭一个實例。欲救著愛動時間軸（加一條
新 cue），毋是切開就好。

**「既有kā播出端寫法訂正掉」猶未決定**（見「五」）：20210206_037午
六條（全形括號五條、`者老` 一條）、20210201_032午 cue 462（`者老`）。
改ê話是直接改交付內容，愛先講。


**`verify_band.py` 的判準已經有五個實測案例**（見上面那張表），涵蓋「有紅帶
／沒紅帶／紅帶在 region 外 3 px／紅帶低位 y=917（1 月卑南，判準放寬時
推理的那個案例，實測通過）／故意套錯 preset」。判準本身另有合成剖面
單元測試（`tests/news/test_verify_band.py`）。還沒涵蓋的是**紅帶
只出現一部分時間**的檔案：判準對這種情形會變弱而不是失效（分子分母一起縮），
但沒有實際檔案可以驗。真遇到就要改成逐幀量，不要靠平均。



burned-in 字幕**幾乎都是中文**，族語主要存在於聲音，以及 `.rtf` 文稿裡
那些拉丁字母的族語行——`rtf.py` 的 `is_cjk_line()` 目前把它們濾掉了，因為
對「抽中文字幕」沒用。對族語語料來說那才是主體，要用的話從那個函式著手。

**不過字幕帶本身也會出現羅馬字**，量過是 51,165 格裡的 231 格（0.45%，
分布在 20 集）：地名（`Pacefongan`、`Kanavirangana`）、歌名
（`Les Mamans du Congo`）、人名，還有整句族語（`koko' yako mali' lobih ila`、
`falohay a mihecaan`）。這些既有語料本來就收了，所以**不要把「非漢字」當成
「不是字幕」濾掉**。20210203_034晚 那格（`Tayra i dao'c`，2112.26–2115.00）
既有是空的，就是漏收的一格。
