# Tasks: recalibrate-news-gates

## 1. verify_band 全量重量與 SPIKE 重定

- [x] 1.1 63 支存檔 mkv 全量跑現版量測（nice、背景發動、`&&` 串收集），逐支記（檔名、比值、判定）
- [x] 1.2 分析兩群分布：有間隙→取中點定新 SPIKE 並記邊界樣本；重疊→帶分布回報使用者議改判準（本 task 以「有結論」為完成，不以「有新值」為完成）
- [x] 1.3 寫測試（紅）：「判無紅帶時輸出含比值與門檻值」（合成 fixture）；改 `verify_band.py` 讓它綠
- [x] 1.4 門檻新值（若有）套用前全量回測：63 支判定不變壞；量測表與樣本數記入 `scripts/news/README.md` 量測紀錄節

## 2. blank_runs 切緣墨判準

- [x] 2.1 寫測試（紅）：合成圖條 fixture——頂端切緣墨的短空白段列入複查清單；無切墨的更長空白段不誤報；長度只影響排序
- [x] 2.2 實作切緣墨量測（讀既有 strips，離線）讓測試綠
- [x] 2.3 實錄回測：006 的 12 條病灶全數列入、054–059 良性空鏡零誤報；證據門檻與校準樣本數記入量測紀錄
- [x] 2.4 若 2021-01 批已在校讀，對該批補跑一次帶外字幕偵測並把複查清單交使用者

## 3. blind_cues 參數化與侷限記錄

- [x] 3.1 寫測試（紅）：`LONG` 可由 CLI 參數覆蓋、預設 6.0 不變
- [x] 3.2 實作參數化讓測試綠；兩類結構性漏抓與 `spread` 誤報實測寫進 docstring 與 README

## 4. 驗收

- [x] 4.1 `tox -e unittest`、`tox -e flake8`、`rebuild --verify`、`name_catalogue --check` 全綠（本 change 不動 store，verify 應原樣通過）
- [x] 4.2 向使用者回報三個門檻的新校準值、樣本數與遺留侷限
