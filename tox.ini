[tox]
skipsdist = True

[flake8]
max-line-length = 79
exclude =
    .git
    .tox
    venv
    kithann

[testenv:yamllint]
deps =
    yamllint
commands =
    yamllint .

[testenv:flake8]
deps =
    flake8
commands =
    flake8 . --show-source --count

[testenv:pymarkdown]
deps =
    pymarkdownlnt
commands =
    pymarkdown \
        --strict-config \
        --disable-rules md013,md029 \
        scan .
    pymarkdown \
        --strict-config \
        --disable-rules MD041 \
        scan -r .github/*

[testenv:shellcheck]
allowlist_externals =
    bash
commands =
    bash shellcheck.sh

[testenv:test]
deps =
    -rrequirements.txt
commands =
    python -m unittest {posargs}

[testenv:behave]
deps =
    -rrequirements.txt
    behave
commands =
    python -m behave --no-skipped {posargs}

# subtitle     單元測試，只需要 numpy + Pillow，CI 可以直接跑。
# subtitle-e2e 另外把已知的 SRT 燒進合成影片再抽回來比對，
#              需要系統套件：ffmpeg、tesseract-ocr、tesseract-ocr-chi-tra、
#              fonts-noto-cjk（.travis.yml 內以 apt addons 安裝）。
[testenv:subtitle]
deps =
    numpy
    Pillow
commands =
    python {toxinidir}/.claude/skills/video-subtitle-srt/scripts/selftest.py \
        --quick

[testenv:subtitle-e2e]
deps =
    numpy
    Pillow
commands =
    python {toxinidir}/.claude/skills/video-subtitle-srt/scripts/selftest.py
